# L22-01 — 我如何知道调用者是谁？身份认证、权限决策、密码凭据与 Bearer Token 校验

> **Canonical Module**: M22 — Security Synthesis II: Authn/Authz & Secure Composition
> **Canonical Title**: L22-01 — How do I know who is calling? (Authentication, authorization, password verifiers, sessions, tokens, and OAuth 2.1)
> **Canonical Module Entry**: Hard `M21` (Security Synthesis I: Trust & Crypto Use), `M11` (Network Applications & TLS/HTTP), `M12` (Web Architecture & Same-Origin); Soft `M19` (Modern Infrastructure)
> **Canonical Lesson Predecessors**: `L21-02` (Cryptographic Primitives & Digital Signatures), `L11-02` (HTTP Protocol & State)
> **Canonical Primary Competencies**: `Judge`, `Explain`
> **Canonical Formal Concept Revisits**: `EC-CON-017 Trust Boundary` (First Home: M07 `L07-01`), `EC-CON-007 Specification` (First Home: M02 `L02-03`)
> **First-Home Claims**: 零第一原籍授权（Zero First-Home Claims Authorized; 零新增 Concept ID）

---

## 1. 学习者核心问题 (Learner Question)

> “在构建一个 Web API 或微服务系统时，当一个 HTTP 请求打到服务端口并携带了一个 Token 时，我到底凭什么确信‘调用者是谁’？为什么‘证明你是谁’（Authentication）和‘你能访问什么数据’（Authorization）是两个截然不同、不可混淆的步骤？拥有合法的 Token 签名，难道不就等于用户有权读取数据吗？为什么数据库绝对不能存储明文密码，甚至连快速哈希值（如 SHA-256）都不能存？为什么所谓‘无状态’的 Bearer Token 一旦签发，就无法在不依赖外部状态的情况下立即吊销？OAuth 2.1 到底是在解决身份认证，还是在做权限授权？”

在学习了密码学基石（`M21`）、TLS 与 HTTP 状态（`M11`）以及浏览器同源策略（`M12`）后，许多初学者经常陷入一系列严重的系统级认知混淆：
1. **认证与授权混淆（Authn vs Authz Conflation）**：误以为验证了用户的密码或 Token 签名，就等于完成了安全检查，忽视了资源访问控制（导致严重的 IDOR / BOLA 漏洞）；
2. **凭据、身份与权力混为一谈（Credential vs Identity vs Authority）**：误以为拥有一个令牌（Credential）就等于实体本身（Identity），更误以为实体本身天生具有操作权力（Authority）；
3. **快速哈希充当密码凭据的灾难**：误以为用 SHA-256 加密/哈希密码就很安全，不知道现代 GPU/ASIC 硬件能够以极其庞大的并发速度暴力离线碰撞无盐或快哈希；
4. **无状态令牌的“免死金牌”幻觉**：误以为 JWT 等无状态令牌既拥有无状态的高性能，又能像服务端 Session 一样随时一键登出并全网即时吊销；
5. **OAuth 角色倒置**：误以为 OAuth 2.0/2.1 是一种“单点登录身份认证”，忽视了它本质上是资源访问授权委托（Authorization Delegation）框架。

本课带领学习者跨越表象，建立严谨的**身份认证与权限决策双阶段解耦模型**，掌握符合现代规范（NIST SP 800-63B-4、RFC 9700 / BCP 240、RFC 7519 / 8725）的凭据校验与工程权衡。

---

## 2. 能力跃迁 (Capability Transition)

| 维度 | 学习前状态 | 学习后状态 |
| :--- | :--- | :--- |
| **认证与授权概念分界** | 模糊地将登录、权限、鉴权通称为“Auth”，写在同一个函数或中间件里 | 严格在架构与代码层面切分 **Authn（证明身份）** 与 **Authz（评估权限）**，掌握凭据校验失败（HTTP 401）与权限不足（HTTP 403）的精确状态语义 |
| **密码凭据存储设计** | 认为 `md5(pass)` 或 `sha256(pass)` 足够安全，或者把可逆密钥当成加盐 | 精确掌握 NIST SP 800-63B-4 规范：使用 CSPRNG 生成唯一盐（>= 32 bits，最小化碰撞），采用可调节工作因子的计算密集型（PBKDF2）或内存密集型（Argon2id）慢速 KDF，使用 `hmac.compare_digest` 防御计时攻击 |
| **令牌校验与剖析能力** | 盲目接受客户端传来的任意 JWT，或者只验证签名就放行数据操作 | 掌握严格的令牌校验剖析文件（`TeachingProfile-BearerV1`）：强制白名单算法、严防 `alg: none`、核对有效期（`exp`）、核对受众（`aud` 防止令牌替换攻击），并在业务层独立执行资源 ACL 检查 |
| **有状态会话 vs 无状态令牌权衡** | 盲目跟风“无状态 JWT 淘汰 Cookie Session”，忽视令牌吊销与网络开销 | 能站在工程架构高度权衡：理解集中式 Session（即时吊销、中心存储压力）与分布式 Bearer Token（分布式自校验、撤销延迟高、凭据盗用风险）的本质代价 |
| **授权委托（OAuth/PKCE）心智** | 以为 OAuth 只是微信/GitHub 登录弹窗，不理解其底层授权协议机制 | 掌握 RFC 9700（BCP 240）与 OAuth 2.1 核心流向：理解授权码模式与 PKCE（代码交换证明密钥）如何防止公开客户端授权码拦截，明确区分 OAuth 授权与 OIDC 身份认证 |

---

## 3. 机制模型 (Mechanism Model)

### (1) 认证与授权的双阶段请求决策路径

计算系统中的安全访问控制由两个先后发生、职责解耦的阶段构成：

```
====================================================================================================
           FIG-M22-01: AUTHENTICATION VS AUTHORIZATION REQUEST DECISION PATH
====================================================================================================

  [ Incoming HTTP Request: Bearer Token / Session Cookie ]
                     │
                     ▼
       ┌───────────────────────────┐
       │ 1. 签名与格式校验         │ ─── 签名伪造 / 损坏 / alg: none ───► [ HTTP 401 Unauthorized ]
       │    (Signature / MAC OK?)  │
       └─────────────┬─────────────┘
                     │ 验证通过
                     ▼
       ┌───────────────────────────┐
       │ 2. 时效与状态校验         │ ─── exp 已过期 / 处于吊销黑名单 ────► [ HTTP 401 Unauthorized ]
       │    (exp > now? Revoked?)  │
       └─────────────┬─────────────┘
                     │ 验证通过
                     ▼
       ┌───────────────────────────┐
       │ 3. 受众与签发者校验       │ ─── aud != 本服务 / iss 不受信 ────► [ HTTP 401 Unauthorized ]
       │    (aud == this_service?) │
       └─────────────┬─────────────┘
                     │ 验证通过
                     ▼
         [ 确立已认证身份 (Authenticated Identity): Principal = "User #42" ]
         ================== 身份认证阶段完成 (Authn Complete) ==================
                     │
                     ▼
       ┌───────────────────────────┐
       │ 4. 资源权限决策 (Authz)   │
       │    Principal 对目标资源   │ ─── 无权限 (e.g. 试图读他人私有文档) ──► [ HTTP 403 Forbidden ]
       │    是否有执行指定操作权力?│
       └─────────────┬─────────────┘
                     │ 授权通过 (Authorized)
                     ▼
         [ 执行业务数据逻辑，返回数据: HTTP 200 OK ]

  * 核心公理 1: Authentication (401) 回答 "你是谁？凭据是否有效？"
  * 核心公理 2: Authorization  (403) 回答 "我知道你是谁，但你是否有权访问这行数据？"
  * 核心公理 3: 拥有有效签名仅证明令牌未被篡改，绝不等于已获得资源操作授权！
====================================================================================================
```

### (2) 密码凭据存储机理与慢速 KDF（NIST SP 800-63B-4）

密码本身是低熵的人类记忆口令。如果攻击者通过 SQL 注入或物理备份泄露拿到了用户口令库：
- **明文存储**：直接导致灾难性凭据泄露与全网撞库；
- **快速未加盐哈希（如 `sha256(password)`）**：攻击者利用预先计算好的彩虹表（Rainbow Table）或现代 GPU 集群（每秒可计算数十亿次 SHA-256，具体硬件速率取决于特定芯片架构与实现），几分钟内即可破解大量口令；
- **通用加密（AES 存储密码）**：密钥一旦泄露（或在同一机器内存中），全量密码瞬间被全量解密。

#### 正确标准：加盐慢速密钥派生函数（Slow Password KDF）
根据 **NIST SP 800-63B-4 §3.1.1.2**：
1. **加盐（Salt）**：每个凭据必须在注册时通过强随机源（CSPRNG 如 `secrets.token_bytes`）独立生成至少 32 bits（4 字节；实践中推荐 16 字节 / 128 bits）的唯一盐。盐的作用是**消除彩虹表预计算优势**，并确保两个密码相同的用户拥有完全不同的哈希存储记录。**盐不需要保密，但必须唯一**；
2. **慢速计算（Compute-Hard / Memory-Hard）**：
   - **计算密集型（Compute-Hard）**：如 **PBKDF2**（NIST SP 800-132 / RFC 8018）。通过循环迭代数万甚至数十万次哈希运算，强制增加单次验证的 CPU 开销；
   - **内存密集型（Memory-Hard）**：如 **Argon2id**（RFC 9106）与 scrypt（RFC 7914）。不仅要求 CPU 运算，还强制占用固定大小的物理内存，使得并行 ASIC 和 GPU 的硬件破解成本呈指数级上升；
3. **记录序列化**：通常格式为 `algo$params$salt$hash`（例如 `pbkdf2_sha256$iterations=100000$<salt_hex>$<hash_hex>`）；
4. **恒定时间比对**：验证时必须使用语言底层提供的常数时间比对函数（Python 标准库 `hmac.compare_digest`），防止攻击者利用字符逐字节对比时的纳秒级时间差进行侧信道攻击。

### (3) Bearer Token 剖析与校验规约（RFC 7519 / RFC 8725）

在分布式与微服务架构中，**Bearer Token（持有者令牌）**被广泛用于无状态服务间调用。
本课程定义了严格的教学参考配置文件 **`TeachingProfile-BearerV1`**（纯标准库实现），包含三个由点号连接的 Base64URL 串：
`Header.Payload.Signature`

```
Header:  {"alg": "HS256", "typ": "JWT", "profile": "TeachingProfile-BearerV1"}
Payload: {"sub": "alice", "aud": "document-service", "iss": "idp.local", "iat": 1757000000, "exp": 1757000300}
```

校验器必须逐项执行强制校验，任何一项不满足必须立刻以安全失败（Fail-Closed）拒绝：
1. **严格算法白名单（Reject `alg: none`）**：校验器必须拥有固定的受支持算法配置（如仅允许 `HS256`）。绝不能根据 Token Header 声明的 `alg` 动态改变验证逻辑！如果 Token 声明 `alg: "none"`，必须无条件拒绝；
2. **密码学完整性校验**：使用本地配置的私钥或共享密钥，独立计算签名并调用 `hmac.compare_digest` 核对。签名不匹配说明令牌在传输中被篡改；
3. **时效校验（`exp`）**：`exp` 必须为有效时间戳，且 `now < exp`；
4. **受众严格绑定（`aud`）**：必须校验 `aud` 严格等于当前服务的服务标识符。如果用户拿着给“计费服务”（billing）签发的有效 Token 来请求“文档服务”（document），必须予以拒绝。否则将引发**令牌替换跨服务攻击（Token Substitution Attack）**；
5. **主体提取与授权接力（`sub`）**：认证成功后，仅代表请求者是合法主体 `alice`。中间件必须将主体传递给具体的业务授权策略引擎。

### (4) 状态化 Session vs. 无状态 Token 的架构抉择

| 架构维度 | 状态化服务存储 Session (Cookie + Store) | 无状态签名 Bearer Token (JWT) |
| :--- | :--- | :--- |
| **状态持有者** | 服务端（集中式数据库、Redis、内存） | 客户端（由客户端在请求头中携带发送） |
| **即时吊销能力** | **即时可吊销**：服务端直接删除 Session 记录，后续请求立刻失效 | **无法即时撤销**：只要私钥没换且 `exp` 没到期，任何拿到令牌的人都能继续通过校验 |
| **吊销应对方案** | 天生支持，无需额外机制 | 必须引入带外状态：短有效期（如 5 分钟）+ Refresh Token、黑名单（Revocation List）、版本代际号（Epoch Bumping） |
| **跨域与移动端** | 受浏览器同源策略与 Cookie 作用域限制 | 天生适合移动客户端、第三方 CLI 与多域名 API 网关 |
| **环境特权风险** | Cookie 默认被浏览器自动附加，极易遭受 CSRF 攻击 | 存储在 LocalStorage 易遭 XSS 窃取；必须由前端显式放入请求头 |

### (5) OAuth 2.1 授权码模式与 PKCE 架构（RFC 9700 / RFC 7636）

**OAuth 2.0/2.1 绝不是身份认证协议，而是授权委托框架（Authorization Delegation Framework）**。
例如：用户允许“图片打印应用”（Client）访问用户存储在“云相册”（Resource Server）里的照片，而无需把云相册的用户名密码交给打印应用。

#### 为什么公开客户端（SPA、手机 App）必须强制使用 PKCE？
根据 **RFC 9700 / BCP 240**（2025 年正式确立的 OAuth 2.0 安全最新现行最佳实践）与正在推进的 **OAuth 2.1 草案**：
- 传统的“密码凭据直接提交模式”（Resource Owner Password Credentials, ROPC）**严禁使用（MUST NOT）**；
- 传统的“隐式授权模式”（Implicit Grant）**不应使用（SHOULD NOT）**，在 OAuth 2.1 中已被移除；
- 所有客户端（特别是无法安全保管客户端密钥 Client Secret 的公开客户端）必须采用**授权码模式 + PKCE（Proof Key for Code Exchange，RFC 7636）**。

```
====================================================================================================
               FIG-M22-02: OAUTH 2.1 AUTHORIZATION CODE FLOW WITH PKCE
====================================================================================================

  Client Application (SPA / App)          Browser / User Agent         Authorization Server (IdP)
       │                                         │                                    │
       │ 1. 动态生成强随机 code_verifier        │                                    │
       │    计算 code_challenge = S256(verifier) │                                    │
       │    本地保存 code_verifier               │                                    │
       │                                         │                                    │
       │ 2. 重定向至授权端点 (携带 challenge) ──►│                                    │
       │    GET /authorize?client_id=...         │                                    │
       │    &code_challenge=...&method=S256 ────►│ 3. 浏览器发起授权请求 ────────────►│
       │                                         │                                    │
       │                                         │ 4. 用户登录并确认授权范围 (Consent) │
       │                                         │◄── 提示用户输入凭据与勾选授权 ─────┤
       │                                         │                                    │
       │                                         │ 5. 302 重定向携带 Authorization Code│
       │ 6. 捕获重定向，提取 auth_code ◄─────────┼─── Location: .../cb?code=XYZ... ───┘
       │                                         │
       │ 7. POST /token (直接后端通道，携带 auth_code + code_verifier) ──────────────►│
       │                                                                              │ 8. PKCE 校验:
       │                                                                              │    计算 S256(verifier)
       │                                                                              │    核对是否等于原 challenge!
       │                                                                              │    (证明是同一个客户端发起)
       │ 9. 颁发 Access Token (JSON 响应) ◄───────────────────────────────────────────┤
       │
       ▼
  * 防御机理: 恶意应用即使在操作系统层窃听捕获了 auth_code，由于拿不到内存中的 code_verifier，
    向 /token 端点换取 Token 时必遭拒绝！
====================================================================================================
```

---

## 4. 论断分层 (Claim Layer)

| 序号 | 论断内容 | 性质 | 验证方式 |
| :--- | :--- | :--- | :--- |
| **C1** | 身份认证（Authn）确认的是“谁在调用”；资源授权（Authz）决定的是“该身份是否有权执行该操作” | **形式规范 (Specification)** | 检查接口返回状态码：身份验证失败为 HTTP 401，权限不足为 HTTP 403 |
| **C2** | 单向快速哈希（如 SHA-256）设计用于数据流完整性，无法抵御离线暴力字典碰撞，不能直接用于密码存储 | **工程事实 (Engineering Fact)** | 查阅 NIST SP 800-63B-4 与现代硬件算力现实，使用 PBKDF2/Argon2id 慢速算法替代 |
| **C3** | 密码加盐（Salt）的根本目的是消除彩虹表预计算并防止碰撞，盐本身无需保密，但必须保证随机且唯一 | **密码学原理 (Cryptographic Principle)** | 验证同一密码在两次加盐哈希后生成的派生值完全不同 |
| **C4** | 令牌拥有有效签名仅证明持有对应密钥者签发了该令牌，并不证明签发者具有受信任地位，也不代表请求已被授权 | **形式规范 (Specification)** | 在服务中模拟签名有效但受众（`aud`）不匹配或角色权限不足的拒绝场景 |
| **C5** | 纯无状态 Bearer Token 在不维护服务端带外状态的情况下，无法实现即时吊销 | **物理现实 (Physical Reality)** | 模拟已签发令牌在失效时间前在网络中重放，只要签名和时间有效，无状态端点必然接受 |
| **C6** | OAuth 2.0/2.1 是授权委托框架，不提供用户身份语义；OpenID Connect 才是建立在 OAuth 之上的身份层 | **形式规范 (Specification)** | 查阅 RFC 6749、RFC 9700 及 OIDC Core 1.0 规范定义 |

---

## 5. 先预测后观察 (Prediction-Before-Observation)

在运行 `labs/foundations/m22/activity_l22_01.py` 之前，请在空白纸上写下你的预测：

1. **预测 1（算法敏捷性陷阱）**：如果一个攻击者将有效 Token 的 Header 改为 `{"alg": "none", "typ": "JWT"}`，并将签名部分清空发送给服务端，一个未实现白名单限制的简单服务会做出什么反应？
2. **预测 2（受众混淆陷阱）**：假设你开发了一个微服务体系，计费微服务（`billing`）和文档微服务（`doc`）共享同一个认证中心签名密钥。如果客户端拿着计费服务的有效 Token 发给文档服务，而文档服务**省略了 `aud` 检查**，会发生什么？
3. **预测 3（权限独立性）**：一个拥有合法有效签名的普通用户 `alice`，向接口发送请求 `DELETE /api/documents/top-secret`，如果不做独立的资源授权校验，系统会发生什么？

> 动手实验后，对照实际观察结果，体会“验证身份绝不等于赋予权力”的深刻内涵。

---

## 6. 动手实验验证 (Hands-on Activity)

本实验运行完全课程自有、本地合成的教学组件 `labs/foundations/m22/activity_l22_01.py`。

### (1) 步骤一：密码凭据格式化与常数时间比对

```bash
python labs/foundations/m22/activity_l22_01.py
```

观察控制台输出中密码凭据的生成过程：
```python
verifier = generate_password_verifier("CorrectHorseBatteryStaple-2026!", iterations=100000)
# 输出格式: pbkdf2_sha256$iterations=100000$<salt_hex>$<hash_hex>
```
验证：
- 正确密码：`verify_password` 返回 `True`；
- 错误密码：`verify_password` 返回 `False`；
- 使用 `hmac.compare_digest` 确保字符串比对时间不泄露首个失配字节位置。

### (2) 步骤二：教学令牌剖析与防御测试

在代码中测试 `TeachingTokenAuthority`：
1. **正常令牌颁发与校验**：观察颁发的 Token 结构；
2. **伪造签名攻击**：修改 Payload 中的用户名，保留原签名，观察校验器抛出 `signature verification failed` 并拒绝；
3. **`alg: none` 攻击**：强制签发无算法无签名令牌，观察校验器抛出 `Rejected algorithm: 'none'` 并拒绝；
4. **受众替换攻击**：签发受众为 `billing-service.local` 的令牌，提交给要求 `document-service.local` 的校验器，观察校验器抛出 `Audience mismatch` 并拒绝。

### (3) 步骤三：解耦的资源授权决策评估

观察控制台第三部分：
- 用户 `alice` 成功完成身份认证；
- 当 `alice` 请求读取 `doc-101` 时，策略引擎评估通过（`read:doc-101` 命中）；
- 当 `alice` 请求写入 `doc-101` 时，策略引擎明确返回 `Forbidden: lacks permission`。

---

## 7. 受控破坏与失效模式 (Controlled Break / Failure Mode)

为了深刻理解防御机制的不可或缺，进行以下受控破损测试：

1. **破损场景 A：禁用 `aud` 检查（受众混淆 / Token 替换攻击）**
   - *破坏动作*：在校验器中注释掉 `if aud != self.expected_audience:` 判定；
   - *观察现象*：原本只被授权调用低权限服务（如公开查询微服务）的客户端令牌，成功被高权限服务（如用户管理服务）接受；
   - *安全结论*：如果服务共用密钥（或信任同一个 IdP），没有受众锁定的令牌可以在任意内部微服务之间任意穿透使用，造成严重的特权跨越！

2. **破损场景 B：接受客户端自选算法（算法混淆攻击）**
   - *破坏动作*：允许客户端传入任意 `alg`，如果 `alg == "none"` 则直接跳过签名校验；
   - *观察现象*：攻击者只需在客户端随意构造任意 Payload（如 `sub: "admin"`），签名留空，即可完全绕过身份检查；
   - *安全结论*：绝不能由非受信输入决定服务端所采用的安全算法标准。

---

## 8. 学习者证据要求 (Learner Evidence)

完成本课学习后，学习者必须在 `course/evidence/foundations-m22-evidence-template.md` 中独立记录以下非易失性事实：

1. **Section B (Authn/Authz 决策路径)**：记录从凭据校验到时效校验、受众校验、身份确立再到资源授权评估的 6 步决策矩阵；
2. **Section C (密码凭据规范)**：记录加盐长度（>= 32 bits）、PBKDF2 计算密集型工作因子参数的设定依据，并说明为什么该参数必须随时间与硬件发展持续调高；
3. **Section D (令牌校验剖析矩阵)**：记录 `TeachingProfile-BearerV1` 中 `alg`, `sub`, `aud`, `exp`, `iat` 的验证规则与非保证声明；
4. **Section E (Session vs Token 权衡分析)**：详细阐述状态化存储与无状态令牌在即时吊销、中心存储压力与网络开销上的本质权衡。

---

## 9. 常见认知误区 (Common Misconceptions)

- **误区 1**：“只要对密码做了 SHA-256 哈希，存入数据库就是绝对安全的。”
  - *正解*：错误。SHA-256 是为高吞吐数据流设计的快速哈希。攻击者利用专业 GPU/ASIC 可以极速尝试海量候选密码。必须使用带唯一随机盐的慢速密码派生函数（PBKDF2 或 Argon2id）来增加碰撞成本。
- **误区 2**：“PBKDF2 是内存密集型（Memory-Hard）算法。”
  - *正解*：错误。PBKDF2 仅仅是计算密集型（Iteration-Hard），它几乎不需要占用内存。专用集成电路（ASIC）可以极低成本高度并行化实现 PBKDF2。真正需要大量内存抵抗硬件加速的是 Argon2id（RFC 9106）与 scrypt。
- **误区 3**：“一个拥有合法有效签名的 JWT，证明用户已经被授权访问系统资源。”
  - *正解*：错误。签名只证明该令牌是由合法密钥持有者签发的、且内容没有被篡改。它既不证明签发者当前是否受信，更不代表该用户有权访问特定业务资源。必须由每个业务端点在认证后独立执行资源授权（Authorization）判定。
- **误区 4**：“无状态 JWT 可以在不查数据库的情况下被随时吊销。”
  - *正解*：错误。无状态性的本质就是服务端不记录状态。要在过期前吊销无状态令牌，必须引入中心化的带外状态（如黑名单、版本号或 Redis 记录），此时系统事实上又退化为了有状态系统。

---

## 10. 暂时可以忽略的技术细节 (What You Can Ignore — For Now)

1. **企业级 IdP（Keycloak, Okta, Active Directory）的复杂配置**：本课聚焦认证与授权的本质协议模型，无需搭建庞大的企业身份服务器；
2. **FIDO2 / WebAuthn 硬件密钥底层协议**：生物识别与硬件物理安全通道属于深度展开内容，不影响对认证决策路径的理解；
3. **复杂的细粒度 ABAC / Zanzibar 图权限引擎实现**：掌握基于角色与资源标记的基础授权逻辑即可，分布式权限评估引擎将在高级架构中展开。

---

## 11. 渐进式支持阶梯 (Progressive Support Ladder)

- **思考问题 (Question)**：如果一个系统签发了有效期为 2 小时的无状态 Bearer Token，当某个员工在 10 分钟后离职、管理员在后台注销了该员工账号，为什么该员工持有的 Token 仍然能够成功调用微服务接口？
- **提示 1 (Hint 1)**：微服务在验证 Bearer Token 时，需要查询中央数据库吗？
- **提示 2 (Hint 2)**：无状态令牌的校验依赖于什么？只要令牌里的 `exp` 还没有到期、签名是合法的，单独的微服务如何知道管理员执行了注销操作？
- **预期观察 (Expected Observation)**：微服务独立执行本地签名和时间戳计算，发现一切合法，因而继续放行请求并返回数据。
- **完整解释 (Full Explanation)**：
  <details>
  <summary>展开查看深入解析</summary>

  这就是无状态 Bearer Token 的固有物理限制：**去中心化的验证便利性是以牺牲即时吊销能力为代价的**。
  每个独立的微服务仅根据私钥/公钥和本地时钟校验令牌，根本不知道远端中心数据库发生了账号注销事件。
  解决这一问题必须引入妥协方案：
  1. **极短有效期 + Refresh Token**：将 Access Token 有效期压缩至 5~15 分钟，要求客户端定期拿 Refresh Token 去中心服务换新；
  2. **吊销黑名单（Revocation List）**：在分布式缓存（如 Redis）中维护已被撤销的 `jti`（Token 唯一编号），但这打破了纯无状态假设；
  3. **代际版本号（Epoch Bumping）**：用户记录中维护 `token_version`，修改密码或离职时版本号加 1，微服务在网关层校验版本。
  </details>

---

## 12. 图解规范与说明 (Visual Specification)

本课配有两份完全课程自有、可编辑的 Mermaid 原生架构图：

1. **`FIG-M22-01-authn-vs-authz-decision-path.mmd` (Visual M22-V1)**：
   - 描述从 HTTP 请求到达、签名检查、时间有效性检查、受众检查、身份确立，到最终执行独立资源授权策略的完整决策流；
   - 明确标注 401（未认证）与 403（未授权）的精确状态边界。
2. **`FIG-M22-02-oauth21-pkce-architecture.mmd` (Visual M22-V2)**：
   - 描述 OAuth 2.1 规范中客户端、用户代理、授权服务器之间的授权码模式与 PKCE 交互序列；
   - 清晰展示 `code_verifier` 与 `code_challenge` 的绑定关系，解释为什么 PKCE 能彻底防御授权码拦截攻击。

---

## 13. 达标通关标准 (Exit Criteria)

学习者必须同时满足以下客观标准方可判定为本课通关：

- [ ] **自动化验证标准**：运行 `python -m unittest labs/foundations/m22/test_m22.py`，全部通过 L22-01 相关的 7 项单元测试（密码凭据生成、唯一盐、慢哈希验证、错误密码拒绝、签名防篡改、`alg: none` 拒绝、`aud` 不匹配拒绝）；
- [ ] **概念辨析标准**：能脱稿清晰解释 Authentication、Authorization、Credential、Identity、Authority 五者的本质区别；
- [ ] **密码工程标准**：解释为什么 SHA-256 不能用于密码存储，明确指出符合 NIST SP 800-63B-4 的凭据加盐与工作因子要求；
- [ ] **架构权衡标准**：能客观评估有状态 Session 与无状态 Bearer Token 在即时吊销、网络开销与中心存储依赖上的三方权衡；
- [ ] **证据模板记录**：在 `course/evidence/foundations-m22-evidence-template.md` 中完整填写 Sections A, B, C, D, E, F。

---

## 14. 能力等级对齐 (Competency Mapping)

- **`Judge` (Primary)**：
  - 针对具体的业务场景（如银行转账系统 vs 开放 API 服务），评估并裁决应采用有状态 Session 还是无状态 Bearer Token；
  - 权衡密码哈希工作因子参数对服务器登录延迟与抗暴力碰撞强度之间的矛盾；
  - 判定授权委托协议（OAuth 2.1）在公开客户端架构中的威胁面与 PKCE 实施必要性。
- **`Explain` (Primary)**：
  - 精确解释为什么拥有合法的数字签名并不能证明调用者已被授权访问特定数据；
  - 解释为什么快速哈希对密码保护是灾难性的，阐述 PBKDF2 与 Argon2id 的防御原理；
  - 解释 PKCE 是如何通过 `S256` 变换在不可信传输信道中绑定代码交换请求者的。

---

## 15. 核心概念与重访关系 (Concepts & Revisits)

- **`EC-CON-017 Trust Boundary` (信任边界 - Revisit)**：
  - *第一原籍*：M07 `L07-01`（虚拟内存与内核隔离）。
  - *本课重访*：信任边界存在于客户端与 API 网关之间、API 网关与后端服务之间、以及服务身份与业务数据行级访问之间。认证确立了跨越边界的主体身份，而授权则是信任边界上最核心的门禁规则。
- **`EC-CON-007 Specification` (形式规约 - Revisit)**：
  - *第一原籍*：M02 `L02-03`。
  - *本课重访*：Token 的 Claims Schema（RFC 7519）、密码存储格式规范、OAuth 2.1 规范（RFC 9700）。任何对规约的隐式降级（如接受 `alg: none`）都会导致灾难性安全穿透。

---

## 16. 学术源流与时效性审计 (Provenance & Currentness)

| 权威规范 / 文献 | 版本与状态 | 生效/核对时间 | 本课引用的工程事实与论断 | 推断边界与禁止事项 |
| :--- | :--- | :--- | :--- | :--- |
| **NIST SP 800-63B-4 §3.1.1.2** | Final Publication | 2025 年 7 月（核对：2026-09） | 密码必须加盐存储（盐 >= 32 bits，强随机生成最小化碰撞）；采用批准的 KDF（PBKDF2 compute-hard）或更新指导；单因素口令最小长度 15 字符，多因素最小 8 字符；允许密码最大长度至少 64 字符且不发生静默截断 | 不得将特定迭代次数固化为课程永恒常数；工作因子必须随算力演进持续提高 |
| **NIST SP 800-132** | Recommendation for Password-Based Key Derivation (Dec 2010 Final; 计划修订) | 2010 年 12 月（核对：2026-09） | PBKDF2 的迭代加密计算密集型标准规范 | PBKDF2 并非内存密集型；ASIC 并行硬件仍可对其进行算力加速 |
| **RFC 9106 (Argon2)** | IETF Informational / Crypto Standard | STABLE | 现代内存密集型（Memory-Hard）密码哈希标准，业界首选候选方案 | Python 标准库未内置；需作为可选依赖通过环境能力门禁（Capability Gate）隔离 |
| **RFC 9700 / BCP 240** | Best Current Practice | 2025 年 1 月（核对：2026-09） | OAuth 2.0 Security BCP：公开客户端强制实施 PKCE（MUST）；严禁使用 ROPC（MUST NOT）；不应使用 Implicit（SHOULD NOT）；重定向 URI 必须精确全字符串匹配 | 不得将 OAuth 授权委托混为用户身份认证 |
| **draft-ietf-oauth-v2-1-15** | Active Internet-Draft | 2026 年 3 月（核对：2026-09） | 整合 OAuth 2.1 规范，正式废弃 ROPC 与 Implicit | 草案处于推进演进中，课程讲授核心协议原则，不固化草案临时编号 |
| **RFC 7636** | Standards Track | STABLE | PKCE（Proof Key for Code Exchange）规范，采用 S256 挑战计算 | 无法防御客户端所在物理设备的完全攻陷 |
| **RFC 7519 / RFC 8725** | Standards Track / BCP | STABLE | JSON Web Token 规范与 JWT Security BCP；严禁 `alg: none`，强制受众校验 | 校验签名不等于完成授权；无状态 Token 不具备即时吊销特性 |

---

## 17. 精确推断边界与禁止事项 (Exact Inference Limits)

为确保学术严肃性与工程严谨性，学习者与评审者必须严格遵守以下推断红线：

1. **禁止混同认证与授权**：严禁在代码或回答中出现“用户登录成功，所以可以直接返回任意数据”的逻辑；
2. **禁止宣称 JWT 能够无状态即时吊销**：在未引入中心化状态（如分布式缓存黑名单或版本代际号）前，严禁宣称纯无状态 Token 可以被即时单向撤销；
3. **禁止将工作因子固化为绝对常数**：PBKDF2 的迭代次数或 Argon2id 的内存开销是随环境算力和业务延迟容忍度动态调整的政策参数，绝非课程不变定理；
4. **禁止宣称 OAuth 提供了用户认证**：OAuth 2.1 颁发的是访问资源服务器的委托令牌，若需确立用户身份 Profile，必须结合 OpenID Connect（OIDC）或专用身份层；
5. **禁止在生产中实现自定义加解密原语**：本课只讲授标准安全规约与调用接口（`secrets`, `hashlib`, `hmac`），严禁手写自定义哈希、伪随机发生器或加密算法。
