# L12-04｜执行循环与响应性：事件循环、微任务与卡顿根源（并发预习）

## 学习者问题

你在浏览网页或使用 Web 应用程序时，有时点击按钮页面毫无反应，鼠标指针变成沙漏或旋转光标，整个页面仿佛“冻结”了；几秒钟后，被冻结的页面突然像快进一样一次性响应了刚才的所有点击。

更奇妙的是，有时页面上的文字和输入框明明已经完全卡死，页面角落里的一个 CSS 旋转图标或手指触摸平滑滚动却依然能够丝滑运转！

为什么拥有 8 核甚至 16 核 CPU 的超级电脑，会被网页上短短几行 JavaScript 脚本拖垮？JavaScript 的事件循环（Event Loop）究竟是如何协调任务执行与屏幕画面更新的？

## 为什么重要

在现代系统设计与前端架构中，“响应性（Responsiveness）”是衡量系统交互品质的核心标尺。然而，由于历史概念的模糊和流行文章的误导，许多工程师对浏览器执行机制的认知存在严重偏差：
- **误以为“JavaScript 与浏览器全局单线程”**：忽视了浏览器本身由众多进程（Browser, Renderer, GPU）与大量后台工作线程（Compositor, I/O, Web Workers）组成的真实并发体系；混淆了“Window 代理的渲染主线程单线程”与“整机系统并发能力”；
- **误以为存在机械死板的“一宏任务一微任务一渲染”发条时钟**：将复杂的规范化事件循环调度模型误解为固定死板的齿轮轮转，不知道微任务队列在微任务检查点必须**一次性连续排空（Drain until empty）**，更不知道屏幕渲染是根据屏幕刷新率与页面可见性**按需有条件触发的（Conditional Rendering Opportunity）**；
- **误以为所有屏幕都拥有死板固定的 16.7ms 帧预算**：将 60Hz 传统显示器的刷新间隔作为物理铁律，无法适应当前主流的 90Hz、120Hz 高刷新屏（ProMotion）或可变刷新率（VRR）设备；
- **误以为发生卡顿即是所有动画全部停滞**：不了解渲染主线程与合成器线程（Compositor Thread）的并发协作，不知道哪些 CSS 属性能够绕开主线程卡顿实现流畅动画。

本课是走向并发世界（M15 并发系统）的**领域特定执行模型前哨预习**。本课**不定义**系统级的全局并发概念（`EC-CON-015` 保留给 M15），而是聚焦于 Web 代理环境下的事件循环调度实证。

## 学习目标

完成本课后，你应能：
- **Trace**：清晰追踪 Window/Document 代理范围内的事件循环（Event Loop）运转时序：从多个任务源（Task Queues）中选择并执行单个任务 &rarr; 在微任务检查点完整排空微任务队列（Promise reactions, `queueMicrotask`）&rarr; 评估条件渲染机会（Rendering Opportunity）&rarr; 执行 `requestAnimationFrame`、样式计算、排版与绘制；
- **Observe**：运行 `labs/foundations/m12/event_loop_fixture.py`，实证观测同步代码、Promise 微任务与定时器任务的确定性相对执行次序；观测长任务（Long Task）阻塞主线程计数器时合成器动画的独立运转表现；
- **Diagnose**：准确定位造成页面掉帧与卡顿（UI Jank）的根本原因：长任务霸占渲染主线程，导致离散输入事件与下一次渲染机会被无限期推迟；
- **Estimate**：在明确给定屏幕物理刷新率假设（如 60Hz 对应 ~16.7ms 预算，120Hz 对应 ~8.3ms 预算）的前提下，估算渲染帧预算，并论证为什么现代浏览器不存在硬编码的全局帧时钟；
- **Explain**：阐述将耗时计算分片（Chunking & Yielding）或下沉至 Web Worker 的架构改进思路。

## 前置知识

- **L12-01（硬前置）**：渲染进程主线程与合成架构；
- **L12-02（硬前置）**：概念渲染流水线阶段（Style &rarr; Layout &rarr; Paint &rarr; Composite）；
- **概念约束提醒**：本课仅作为并发机制的前哨预习（Preview），**绝不**在此定义核心概念 `EC-CON-015`（保留至 M15 `L15-01`）。

---

## Mental Model｜代理作用域下的事件循环与并发协作

首先建立清醒的系统定位：**浏览器的事件循环不是全局统一的大时钟，而是绑定在特定代理上下文（如 Window/Document Agent）上的调度循环**。

```
=========================================================================================
WINDOW / DOCUMENT EVENT LOOP (渲染主线程事件循环模型)
=========================================================================================

  +---------------------------+   +---------------------------+   +--------------------+
  | 任务源: 用户交互输入      |   | 任务源: 定时器任务        |   | 任务源: 网络/IPC   |
  | (click, keypress, touch)  |   | (setTimeout, setInterval) |   | (Fetch 响应到达)   |
  +---------------------------+   +---------------------------+   +--------------------+
                |                               |                           |
                +-------------------------------+---------------------------+
                                                |
                                                v
                              [ 1. 从任务队列中挑选并执行 1 个任务 ]
                                                |
                                                v
                                  [ 2. 微任务检查点 (Checkpoint) ]
                                  * 完整排空微任务队列 (Drain until EMPTY)
                                  * 包含: Promise.then(), queueMicrotask()
                                  * 若在微任务中继续添加新微任务，会连环吞吐直到彻底清空！
                                                |
                                                v
                           [ 3. 是否存在渲染机会？(Rendering Opportunity) ]
                                                |
                               +----------------+----------------+
                               |                                 |
                            [ 是 (YES) ]                      [ 否 (NO) ]
                               |                                 |
              +----------------+----------------+                |
              |                                 |                |
              v                                 v                v
     [ 执行 rAF 回调 ]               [ 概念排版与绘制更新 ]     (跳过渲染更新)
     (requestAnimationFrame)         (Style -> Layout -> Paint)          |
              |                                 |                        |
              +----------------+----------------+                        |
                               |                                         |
                               +-----------------------------------------+
                                                |
                                                v
                                  [ 4. 开启下一次循环 (Next Turn) ]
=========================================================================================
                                        ||
           并发通道协作 (Concurrence with Compositor / GPU Thread)
                                        ||
=========================================================================================
+---------------------------------------------------------------------------------------+
|  Compositor Thread (合成器线程):                                                       |
|  - 拥有独立于主线程的执行调度;                                                          |
|  - 直接处理已提升图层的 CSS transform / opacity 动画;                                  |
|  - 主线程陷入长任务卡死时，合成器线程上的变换动画依然可以流畅刷新！                      |
+---------------------------------------------------------------------------------------+
```

> **设计警示（Negative Guardrails）**：
> 1. 绝不要画成“一个任务 &rarr; 一个微任务 &rarr; 一次渲染”的僵化发条齿轮！微任务必须在检查点全部排空；渲染机会只在满足显示刷新周期等条件时才会发生；
> 2. 绝不要声称“JavaScript 全局单线程”：这只是主线程脚本的调度特征，后台的渲染管道与工作线程是高度并发协作的。

---

## Mechanism｜任务、微任务与帧预算的真实物理

### 1. 任务（Tasks）与微任务（Microtasks）的严格时序

WHATWG 规范对任务与微任务定义了明确的先后顺序语义：
- **任务（Task，历史曾俗称宏任务 Macrotask）**：
  来自离散任务源的离散工作单元。例如用户的鼠标点击回调、定时器（`setTimeout`）触发的回调、网络数据读取完成事件。
  **关键规则**：事件循环在每一轮循环中，只从候选任务队列中**选择一个任务**执行。
- **微任务（Microtask）**：
  专为更细粒度的异步反应而设计，典型来源包括：
  1. `Promise` 的成功/失败反应（`.then()`, `.catch()`, `await` 恢复点）；
  2. `queueMicrotask(...)` 显式入队；
  3. `MutationObserver` DOM 变动监听。
  **关键规则**：当一个任务执行完成后，引擎会立即触发**微任务检查点（Microtask Checkpoint）**。在此检查点，引擎会循环取出微任务执行，**直到微任务队列完全被清空**。如果在执行微任务的过程中又派生了新的微任务，新微任务会在同一个检查点被继续执行，直到彻底空空如也！

#### 经典时序实验推理
考虑以下经典代码片段：
```javascript
console.log("1_sync");

setTimeout(() => {
  console.log("2_timer");
}, 0);

Promise.resolve().then(() => {
  console.log("3_promise");
});

console.log("4_sync");
```

根据规范执行时序：
1. 主脚本作为一个初始任务启动，同步打印 `1_sync`；
2. `setTimeout(..., 0)` 向定时器任务队列注册一个定时任务（Timer Task）；
3. `Promise.resolve().then(...)` 向微任务队列注册一个反应回调（Microtask）；
4. 同步代码继续执行，打印 `4_sync`；
5. 当前任务结束，立即触发**微任务检查点**！引擎排空微任务队列，打印 `3_promise`；
6. 检查渲染机会（若此时无渲染需求则跳过）；
7. 事件循环进入下一轮，从任务队列中挑选出定时器任务执行，打印 `2_timer`。

**确定的输出顺序**：`1_sync` &rarr; `4_sync` &rarr; `3_promise` &rarr; `2_timer`。

### 2. 渲染机会（Rendering Opportunity）与帧预算

屏幕不会在每次微任务结束后都盲目重绘一次。渲染更新受制于硬件显示器的物理刷新节奏：
- **垂直同步信号（VSync）**：显示硬件按照固定的物理频率扫描像素。在传统的 60Hz 显示器上，屏幕每秒刷新 60 次，相邻两次刷新的物理间隔约为：
  $$\text{Interval}_{60\text{Hz}} \approx \frac{1000\text{ms}}{60} \approx 16.67\text{ms}$$
- **这绝不是自然铁律！**
  在现代 120Hz 电竞屏或移动设备上，物理刷新间隔为：
  $$\text{Interval}_{120\text{Hz}} \approx \frac{1000\text{ms}}{120} \approx 8.33\text{ms}$$
  如果页面处于后台最小化或隐蔽标签页中，浏览器甚至会将刷新率降低到 1Hz 或完全暂停渲染以节省电力。
- **渲染机会的触发**：当显示器准备好接收下一帧，且文档发生过样式或几何变更时，浏览器在当前任务与其微任务排空之后，才会执行渲染流水线：
  1. 执行 `requestAnimationFrame`（rAF）回调；
  2. 重新计算样式（Recalculate Style）；
  3. 执行排版布局（Layout）；
  4. 生成绘制列表（Paint）；
  5. 提交（Commit）至合成器线程。

### 3. 长任务（Long Task）与 UI 卡顿的物理根源

如果一个任务（例如一个复杂的递归计算、密集的 JSON 解析或大数据循环）在主线程上连续执行了超过 50ms（W3C 规范中将超过 50ms 的任务定义为 **Long Task**），灾难便发生了：
1. **输入被挂起**：用户的点击、按键事件虽然由操作系统内核捕捉并发送到了渲染进程的 IPC 队列，但因为主线程正在忙于执行死循环，事件循环无法挑选下一个输入任务；
2. **渲染被跳过**：即使硬件 VSync 信号频频到来，主线程也无法运行 rAF 或排版更新，导致显示画面出现掉帧（Drop Frame）或完全卡死；
3. **合成线程的独立运转（Compositor Concurrency）**：
   如果页面上有使用 CSS `transform` 或 `opacity` 的动画，且该图层已被提升为独立合成层（GPU Layer），**合成器线程可以直接在 GPU 显存中更新变换矩阵并输出到屏幕**！这就解释了为什么主线程卡死时，CSS 旋转图标往往还能继续旋转，而基于 JavaScript `setInterval` 的动画则会彻底冻结。

---

## Hands-on｜受控本地实验：执行时序与长任务卡顿观测

在配套的 `labs/foundations/m12/event_loop_fixture.py` 中，我们提供了严谨安全的本地实验页面。

### 1. 启动实验服务

```bash
python labs/foundations/m12/event_loop_fixture.py
```

浏览器打开打印出的动态端口地址（`http://127.0.0.1:<port>/`）。

### 2. 实验 Demonstration 1：微任务抢先排空验证

- 点击页面上的 **“Run Task/Microtask Test”**；
- 观察控制台与页面输出：
  `Step 1: 1_sync_start` &rarr; `Step 2: 2_sync_end` &rarr; `Step 3: 3_microtask_promise_1` &rarr; `Step 4: 3b_microtask_promise_chained` &rarr; `Step 5: 3c_microtask_queueMicrotask` &rarr; `Step 6: 4_timer_task (setTimeout 0ms)`；
- **观测结论**：所有链式 Promise 与 `queueMicrotask` 在同步代码执行完后被全量连续排空，严格抢先于 `setTimeout(0)` 定时器任务。

### 3. 实验 Demonstration 2：安全长任务与合成线程并发

- 页面包含两个动态元素：
  1. 由 CSS `animation: spin 2s linear infinite` 驱动的 GPU 独立变换方块（Compositor 线程）；
  2. 由 `requestAnimationFrame` 持续自增计数的数字面板（主线程）。
- 点击 **“Block Main Thread (500ms Bounded Loop)”**（受严格安全阈值保护，绝无死循环）：
  - **观测现象**：数字面板的计数器瞬间停滞 500ms（掉帧卡顿），任何按钮点击无法立刻响应；而在许多现代硬件加速浏览器中，旁边的旋转方块依然在平滑旋转！
- 点击 **“Run Chunked Yielding (500ms Sliced)”**：
  - 代码将 500ms 的计算拆解为多个 50ms 的小切片，并在每片结束后通过 `setTimeout(doChunk, 0)` **主动出让控制权（Yield to Event Loop）**；
  - **观测现象**：整个计算同样完成，但主线程的计数器与用户界面始终保持流畅响应，未发生可感知的冻结卡顿。

---

## Checkpoint Investigation

### Question

为什么在渲染主线程上执行一个耗时 500ms 的同步计算会导致页面按钮点击失去响应并使主线程计数器冻结，而基于 CSS `transform` 的旋转动画却可能依然平滑运转？如果在开发中遇到类似重型计算，应当采取什么工程方案化解该瓶颈？

<details>
<summary>Hint 1</summary>
思考 JavaScript 主代码执行、用户输入事件回调与 requestAnimationFrame 运行在什么线程？它们与合成器线程（Compositor Thread）的关系是什么？
</details>

<details>
<summary>Hint 2</summary>
回顾事件循环中“选择 1 个任务执行”的规则。如果一个任务始终不返回，事件循环能否推进到下一个微任务检查点或渲染更新？如何利用分片（Chunking）或 Web Worker 释放主线程？
</details>

<details>
<summary>Expected Observation</summary>
在受控 fixture 中观察，500ms 的长任务使主线程事件循环被垄断，导致输入事件排队、rAF 计数器停滞，直观表现为 UI 卡顿。但是，因为现代浏览器采用多线程渲染架构，经过图层提升且仅修改 transform/opacity 的动画可以由 Compositor 线程独立于主线程在 GPU 上执行。将计算分片出让（Yielding）或迁移到 Web Worker 可以使主线程保持即时响应。
</details>

<details>
<summary>Full Explanation</summary>
1. <b>卡顿的本质原因</b>：
   在 Window/Document 代理中，JavaScript 执行、DOM 事件调度、样式计算、布局以及 <code>requestAnimationFrame</code> 均受制于渲染主线程（Renderer Main Thread）。事件循环必须等待当前正在运行的任务完全退出，才能处理下一个输入事件或响应屏幕 VSync 刷新信号。一个持续 500ms 的同步计算完全剥夺了事件循环出让给其他任务的可能，导致这半秒内所有用户交互全部发生滞后（Jank）。
2. <b>合成器并发独立性（Compositor Concurrency）</b>：
   现代浏览器并非单线程系统。当元素应用了 <code>transform</code> 并被赋予独立的合成图层后，Compositor 线程持有该图层在 GPU 上的纹理引用。只要动画不改变几何排版（Layout），Compositor 线程即可直接按照时间戳更新矩阵变换并将图块提交给屏幕，完全不需要主线程参与，因此在主线程卡死时依然能保持平滑运转。
3. <b>标准工程解法</b>：
   - <b>时间切片与主动让出（Time Slicing & Yielding）</b>：利用 <code>setTimeout(..., 0)</code> 或 <code>scheduler.yield()</code> 将密集任务切成 &le; 50ms 的细粒度工作单元，将控制权归还给事件循环，保证输入和渲染机会能够及时穿插执行；
   - <b>Web Worker 卸载</b>：通过 Web Worker 将纯粹的数据处理、科学计算迁移到完全独立的后台工作者进程/线程中运行，彻底解脱渲染主线程。
</details>

---

## Common Misconceptions

- **“JavaScript 和整个浏览器都是绝对单线程的。”** 错。浏览器是多进程且高度多线程的体系。即便是单个渲染进程，也拥有 Main Thread、Compositor Thread、Raster Thread、I/O Thread 等众多后台线程。所谓的“单线程”仅是指单个 Window 代理上下文内的用户 JavaScript 脚本默认共享同一个主线程调度循环。
- **“事件循环是僵化机械的‘执行一个宏任务，执行一个微任务，刷一次屏幕’。”** 错。规范明确规定微任务队列在检查点必须被全量清空（可能连续处理几十个微任务）；而渲染更新是条件触发的，只有当硬件刷新时钟到来且页面需要重绘时才会触发，绝非每个任务之后都强制刷屏。
- **“`setTimeout(fn, 0)` 保证在 0 毫秒后立即执行。”** 错。规范中嵌套定时器存在 4ms 的最小限流钳制（Clamping）；更重要的是，定时器回调只是被推入了任务队列，它必须等待主线程上当前正在执行的任务和所有微任务全部执行完毕后，按先来后到被调度。如果当前任务耗时 5 秒，`setTimeout(0)` 也会被延迟 5 秒后才执行。
- **“只要主线程被长任务卡死，页面上所有的 CSS 动画必定全部冻结。”** 错。依赖主线程排版的动画（如修改 width, margin, top）会冻结；但被提升至独立合成图层且仅使用 `transform` / `opacity` 的 GPU 合成器动画可以保持流畅。

## What You Can Ignore—for Now

- WebAssembly 多线程 SharedArrayBuffer 底层 `Atomics.wait` 与互斥锁在浏览器宿主中的线程挂起机制；
- Chrome 调度器内部对不同输入源（Input Priority vs Timer Priority）实施的任务优先级启发式调优算法；
- V8 垃圾回收器并发标记（Concurrent Marking）与增量清除（Incremental Sweeping）的主线程暂停细节。

## Exit Criteria

- 能按准确顺序叙述 Window 事件循环从任务执行、微任务检查点排空到条件渲染机会的完整决策链条；
- 能独立分析包含同步代码、Promise、`queueMicrotask` 与 `setTimeout` 的代码片段并给出准确的确定性相对时序；
- 能从主线程垄断的角度解释长任务造成 UI 卡顿的底层机理，并指出合成线程动画能够保持独立的系统原因；
- 能说明 16.7ms 帧预算背后的物理假设，并指出高刷屏时代的估算差异。

## Competency Mapping

- **Primary**: **Observe** (在受控实验与浏览器中实证观测任务时序、长任务卡顿与合成器并发表现)
- **Growth**: **Diagnose** (诊断排查主线程卡顿根源), **Estimate** (估算特定刷新率下的帧预算与计算切片周期)

## Provenance

- **WHATWG**: *HTML Living Standard* (*Event loops*, *Processing model*), 2026.
- **W3C**: *Long Tasks API 1*, W3C Recommendation.
- **W3C**: *Navigation Timing Level 2*, 2026.
