# L16-02 — 如何安全地调用远程函数？重试风暴、幂等性屏障与事务边界

> **Canonical Module**: M16 — Distributed Systems Foundations: Partial Failure & RPC
> **Canonical Title**: L16-02 — How do I call a remote function safely?
> **Canonical Predecessors**: `L16-01` (Partial Failure & No-Response Ambiguity)
> **Canonical Primary Competencies**: `Trace`, `Judge`, `Explain`
> **Canonical Concept Revisits**: `EC-CON-005 Interface`, `EC-CON-008 Invariant`, `EC-CON-003 Representation`, `EC-CON-007 Specification`
> **First-Home Claims**: 零第一原籍授权（Zero First-Home Claims Authorized）

---

## 1. 学习者核心问题 (Learner Question)

> “既然网络调用超时后我们无法知道服务端到底有没有执行，那我们到底该不该重试？如果不重试，网络抖动就会让本来能成功的请求失败；如果重试，又可能导致扣两次款或重复发货。到底怎样的机制，才能让我们在充满不确定性的网络中，安全地调用一个远程接口？”

在 `L16-01` 中，我们直面了分布式系统冷酷的物理现实：**静默并不揭示远端状态**。客户端本地超时决定停止等待，绝不等于远端操作没有生效。

直觉上，许多初学者会走向两个极端：
- **极端 A（绝对不重试）**：任何网络超时都直接向用户报错。这使得系统极度脆弱，哪怕网络中仅仅发生了 1 毫秒的微小毛刺，用户体验就会彻底中断；
- **极端 B（无脑循环重试）**：只要没拿到成功应答，就一直 `while not success: retry()`。当服务端由于高负载而变慢时，成千上万个客户端的重试请求如潮水般涌来，瞬间形成**重试风暴（Retry Storm）**，将濒临崩溃的服务端彻底击垮，更在数据库中制造海量的重复脏数据。

本课将解决这一经典困境：如何通过**重试预算（Retry Budget）**、**指数退避与抖动（Backoff & Jitter）**，以及核心防线——**幂等性屏障（Idempotency Barrier）**，在不确定的网络之上构筑确定的系统状态。

---

## 2. 能力跃迁 (Capability Transition)

| 维度 | 学习前状态 | 学习后状态 |
| :--- | :--- | :--- |
| **心智模型** | 以为“重试是网络编程的标配万灵药”，到处写死 `retry_times = 3` | 深刻理解重试的危险性，掌握重试放大效应（Amplification）与级联雪崩机理，确立“没有幂等性契约的非只读重试等于主动破坏数据”的心智模型 |
| **算法设计** | 使用固定的 `sleep(1)` 进行线性重试，引发集群级别的同步周期性雷鸣冲击（Thundering Herd） | 掌握全抖动指数退避算法（Exponential Backoff with Full Jitter），能根据调用拓扑与服务等级协议合理配置重试预算与截止期限 |
| **架构与事务** | 误以为“幂等就是服务端返回一模一样的字节”，把幂等表独立于业务库之外导致不一致 | 能在单次事务边界（如 SQLite `BEGIN IMMEDIATE`）内原子绑定幂等键声明与业务状态变迁，清晰定义 TTL 保留期边界与推论边界 |

---

## 3. 机制模型 (Mechanism Model)

### (1) 重试放大效应与雪崩级联 (Retry Amplification & Cascading Collapse)

在分层的微服务架构中，调用通常沿着服务树向下延伸：服务 A 调用服务 B，服务 B 调用服务 C，服务 C 调用数据库 D。

假设每个调用层级在面对超时后，都独立、盲目地配置了“最多重试 3 次”：

```
====================================================================================================
               FIG-M16-02 (TOP): RETRY AMPLIFICATION TREE (k^n MULTIPLICATION)
====================================================================================================

      [ Service A ] (Initiates 1 user request; on timeout retries k=3 times)
            │
            ├───────────────────────┬───────────────────────┐  (3 requests dispatched to B)
            ▼                       ▼                       ▼
      [ Service B_1 ]         [ Service B_2 ]         [ Service B_3 ]  (Each retries k=3 to C)
            │                       │                       │
         ┌──┼──┐                 ┌──┼──┐                 ┌──┼──┐       (9 requests dispatched to C)
         ▼  ▼  ▼                 ▼  ▼  ▼                 ▼  ▼  ▼
       [ C1 C2 C3 ]            [ C4 C5 C6 ]            [ C7 C8 C9 ]    (Each retries k=3 to D)
         │  │  │                 │  │  │                 │  │  │
        27 Requests hammer downstream Database D!                      (Traffic multiplied by 3^3 = 27!)
====================================================================================================
```

如果下游数据库 D 因瞬时锁竞争或慢查询出现短暂延迟：
1. 服务 C 判定超时，对 D 发起 3 倍重试；
2. 服务 B 判定 C 超时，对 C 发起 3 倍重试（产生 $3 \times 3 = 9$ 倍请求）；
3. 服务 A 判定 B 超时，对 B 发起 3 倍重试（产生 $3 \times 3 \times 3 = 27$ 倍请求）；
4. **系统雪崩**：底层的物理负载不是线性增加，而是在模型假设下按指数阶乘膨胀。这种由于重试导致下游负载几何级数放大的现象被称为**重试放大（Retry Amplification）**。

因此，重试必须受到严格的**系统规格（重温 `EC-CON-007 Specification`）**约束：
- **只在最接近故障的一层重试**，不要在整个调用栈的每一层都盲目重试；
- **重试预算（Retry Budget）**：一个进程在单位时间内发起的重试请求数量，绝不能超过总请求量的固定比例（如 10%）；
- **全局截止期限传递（Deadline Propagation）**：如果用户端总截止时间已过，下游服务必须立即放弃重试并快速失败。

### (2) 指数退避与全抖动：打破周期性共振

如果所有客户端在失败后都等待固定的时间（例如固定等待 1 秒）再重试，那么在网络恢复的一瞬间，所有客户端将同时向服务端发起重试，造成**雷鸣冲击（Thundering Herd）**。

为了打散重试流量，系统采用**指数退避（Exponential Backoff）**配合**全抖动（Full Jitter）**（Marc Brooker, AWS 2015）：

$$t_{\text{sleep}} = \text{random}\left(0, \; \min\left(t_{\text{max}}, \; t_{\text{base}} \times 2^{\text{attempt}-1}\right)\right)$$

- **指数递增**：随着连续失败次数的增加，退避等待时间的上限呈指数级递增，给服务端留出恢复喘息的窗口；
- **全抖动（Full Jitter）**：在 0 到当前上限之间均匀随机抽取一个休眠值。数学证明，全抖动能最大程度地把并发客户端的重试时间点平摊到整个时间轴上，将尖峰脉冲转化为平缓的水流。

### (3) 核心防线：幂等性原理 (The Idempotency Principle)

解决重试副作用的唯一根本途径，是让接口具备**幂等性（Idempotency）**。

回顾数学中的幂等映射：一个函数作用两次与作用一次效果相同：
$$f(f(x)) = f(x)$$

在计算机系统中（重温 **`EC-CON-008 Invariant`**），**幂等性是不变式状态迁移**：
> **“一个操作被多次执行所产生的持久业务状态变迁，与单次执行所产生的持久业务状态变迁完全相同。”**

#### 自然幂等 vs. 人工合成幂等：
1. **自然幂等（Natural Idempotency）**：
   - 绝对状态覆写：`SET status = 'CANCELLED'`，无论执行 1 次还是 100 次，订单最终状态都是 `CANCELLED`；
   - 绝对值赋值：`UPDATE config SET max_threads = 64`；
   - 只读查询：HTTP `GET`，不产生业务状态突变；
2. **非自然幂等（Non-Idempotent）**：
   - 相对增量运算：`UPDATE accounts SET balance = balance - 100`；
   - 列表追加：`INSERT INTO transfer_logs VALUES (...)`；
   - 每执行一次，业务状态都在发生新的不可逆累加！
3. **人工合成幂等：引入幂等键（Idempotency Key）**：
   - 对于非自然幂等的写操作，客户端在调用时必须附带一个全局唯一的业务标识符——**幂等键（Idempotency Key / Request ID）**；
   - 服务端必须在处理业务前，利用唯一约束或事务机制记录该幂等键。

### (4) 关键机制：单次原子事务边界中的幂等记录

在服务端设计幂等保护时，最常见的架构败笔是将“幂等记录”与“业务更新”拆分成两个独立的步骤（例如先写 Redis 记录幂等键，再写 MySQL 扣款）。如果写完 Redis 后机器断电，MySQL 从未执行，后续重试却会被 Redis 拦截，导致业务永远无法执行！

**系统不变式铁律**：
> **幂等键的声明、占位、以及受保护的业务状态突变，必须被包裹在同一个原子事务边界内（Atomic Transaction Boundary）。**

```
====================================================================================================
               FIG-M16-02 (BOTTOM): ATOMIC IDEMPOTENCY TRANSACTION WORKFLOW
====================================================================================================

      Client Dispatch                                 Server SQLite Transaction
    (Idempotency-Key: K)                               (BEGIN IMMEDIATE Mode)
            │                                                    │
    Attempt 1 ──────────────────────────────────────────────────>│ [1] Check: SELECT status WHERE key = K
            │                                                    │      -> Not found!
            │                                                    │ [2] Claim: INSERT INTO idempotency_keys
            │                                                    │      (key, status='IN_PROGRESS')
            │                                                    │ [3] Mutate: UPDATE counter = counter + 1
            │                                                    │ [4] Complete: UPDATE status = 'COMPLETED'
            │                                                    │ [5] COMMIT Transaction!
            │ <─── [Response Dropped by Fault Shim!] ─────────── ✘
            │
      (Client Times Out -> Applies Jittered Backoff)
            │
    Attempt 2 ──────────────────────────────────────────────────>│ [1] Check: SELECT status WHERE key = K
   (Same Key: K)                                                 │      -> Found! status == 'COMPLETED'
            │                                                    │ [2] Skip Business Mutation!
            │ <─── Return Cached Prior Response (OK) ────────────│ [3] COMMIT / Close
            │
====================================================================================================
* RESULT: Business state mutated EXACTLY ONCE despite 2 client attempts!                           *
* Invariant f(f(x)) = f(x) preserved within the database transaction boundary!                     *
====================================================================================================
```

### (5) 幂等存储的物理边界：保留期（Retention Horizon / TTL）

很多初学者误以为幂等键能够“保护数据直到宇宙尽头”。
现实中，存储是有限的物理资源。服务端的幂等记录表不可能无限膨胀，它必须具备**保留期（Time-To-Live / Retention Horizon）**（例如保留 24 小时或 7 天）。

**保留期失效风险**：
如果一个网络请求由于极端原因在公网路由节点中滞留或由于客户端 Bug 在 8 天后才重试，而服务端的幂等记录早已在第 7 天被后台任务清理（Eviction），服务端将会把这个旧请求当作全新的请求再次执行！因此，幂等性保证**严格受限于声明的保留期窗口**。

---

## 4. 认知声称层 (Claim Layer)

1. **核心系统不变式 (Core Invariants)**：
   - 幂等性断言针对的是系统持久化状态（`EC-CON-008 Invariant`），**绝不要求网络响应的字节流必须完全逐位一致**（响应中的处理时间戳、跟踪 Trace ID、或者动态签名可以不同）；
   - 幂等性屏障与业务状态更新必须具有原子性因果共存关系；
   - 重试策略必须拥有确定的最大尝试预算（Total-Attempt Budget），严禁在无熔断机制的系统内编写无界重试循环。
2. **具名规范与实证 (Specification & Evidence)**：
   - RFC 9110 §9.2 明确界定了 HTTP 标准方法的幂等规范（`GET`, `HEAD`, `PUT`, `DELETE` 具有幂等规格；`POST` 默认不具有幂等规格）；
   - IETF 正在推进的草案 `draft-ietf-httpapi-idempotency-key-header-07` 为现代 HTTP API 提供了标准请求头 `Idempotency-Key` 的操作模式参考（注：该文档截至 2026 年属于已过期的 Internet-Draft，不可被误当作已发布的正式 RFC 标准引用）；
   - 课程实验在 SQLite `BEGIN IMMEDIATE` 模式下，验证了单连接排他写入与并发冲突处理，证明在同一事务中绑定幂等状态与业务计数可消除重复副作用。
3. **推论边界与当前实践 (Current Practice & Inference Limits)**：
   - 本实验证明的幂等性保证，**严格受限于受控的本地 SQLite 数据库事务边界**，**绝不能外推宣称为支持任意第三方外部系统（如发送物理短信、调用外部第三方支付网关）的绝对“精确一次（Exactly-Once）”神话**；
   - 指数退避与全抖动属于工程最佳实践策略，其底数、上限与种子分布是具体的策略选择，不是普遍的物理定律。

---

## 5. 观察前预测 (Prediction-Before-Observation)

在运行本课实验代码前，请写下你的预测：

1. **预测 A（不带幂等键的重试）**：在客户端配置最多 2 次重试、服务端由于网络故障垫片在第一次执行后丢弃响应的情况下，服务端的原始计数器最终会增加多少？
2. **预测 B（带幂等键的受保护重试）**：在相同的响应丢失与 3 次重试场景下，但客户端每一次重试都携带相同的 `idempotency_key`，服务端计数器最终会增加多少？为什么？
3. **预测 C（并发同时到达）**：如果有 4 个独立的客户端工作线程在同一瞬间发送携带相同幂等键的请求，服务端会不会因为高并发交替而意外执行两次业务逻辑？

---

## 6. 动手实践活动 (Hands-on Activity)

本课使用课程提供的 Localhost RPC 与 SQLite 幂等实验装置：

```bash
# 1. 运行 M16-02 基础活动（包含不安全重试、幂等保护、并发去重与保留期演练）
python labs/foundations/m16/activity_l16_02.py

# 2. 运行自动化单元测试套件
python -m unittest discover -s labs/foundations/m16 -p "test_*.py"

# 3. 清理生成的数据库及临时文件
python labs/foundations/m16/reset.py
```

### 观察要点：
1. **Phase 1（不安全重试）**：确认客户端重试 2 次后，服务端业务计数器从 0 变成了 2，确凿观察到**重复副作用（Duplicate Effect）**；
2. **Phase 2（受保护路径）**：确认在相同的响应丢弃故障下，携带幂等键的客户端哪怕重试了 3 次，服务端业务计数器**恒为 1**，第二次与第三次均命中缓存去重；
3. **Phase 3（并发竞态压测）**：确认 4 个线程并发提交相同键时，计数器增量精确为 1；
4. **Phase 4（保留期边界测试）**：确认当幂等记录超过 TTL（200ms）并被主动清理后，再次发送相同键将重新执行，证实保留期边界是真实的物理限制。

---

## 7. 受控破坏与失效模式 (Controlled Break / Failure Mode)

### 失效示范：将幂等性检查写在数据库事务之外的并发击穿

审查以下看似正确的伪代码：

```python
# 典型设计缺陷：Check-then-Act 跨越了非原子边界
def handle_payment(request):
    key = request.headers.get("Idempotency-Key")

    # [1] 在事务外检查是否已经执行过
    existing = redis_client.get(key)
    if existing:
        return json.loads(existing)

    # [!] 危险窗口：两个相同的重试请求同时并发到达此处！
    # 都发现 existing 为 None！

    # [2] 独立的数据库扣款
    with db.begin_transaction():
        db.execute("UPDATE account SET balance = balance - 100")

    # [3] 写入缓存
    redis_client.set(key, json.dumps({"status": "SUCCESS"}), ex=86400)
    return {"status": "SUCCESS"}
```

1. **破坏现象**：网络发生波动，上游网关重试，两个完全相同的扣款请求以 1 毫秒之差并发到达两台 Web 服务器。两台服务器同时执行步骤 `[1]`，都在 Redis 中未查到该键。随后两台服务器分别执行了步骤 `[2]`，账户被**扣了两次款**！
2. **机理剖析**：这本质上是 `L15-01` 逻辑竞态条件（Check-then-Act）在分布式架构中的重现。检查是否存在与执行业务是两个分离的操作，中间缺乏互斥保护。必须利用数据库的 `UNIQUE` 唯一约束或原子事务锁（`INSERT INTO idempotency_keys ...`）在执行前完成强占位。

---

## 8. 学习者实证记录 (Learner Evidence)

学习者必须在 `course/evidence/foundations-m16-evidence-template.md` 的 **Section D, E, F, G** 中如实记录：
1. 记录不安全路径下的重试次数、响应丢失注入证据、以及计数器最终增量（必须 $> 1$）；
2. 记录保护路径下所使用的具体 `idempotency_key` 字符串，以及在多次重试下计数器增量恒为 1 的实测数据；
3. 记录并发 4 线程压测下的最终计数器状态；
4. 记录 TTL 过期清除后的重执行观测结果；
5. 逐项审查推论边界：确认本实验没有声称跨越任意外部系统的无限“Exactly-Once”。

---

## 9. 常见认知谬误 (Common Misconceptions)

- **谬误 1：“幂等性意味着客户端多次重试拿到的 HTTP 响应必须逐字节一模一样。”**
  - **纠正**：幂等性保证的是系统**内部业务状态的不变式**（$f(f(x)) = f(x)$）。第二次请求返回的响应中，响应头的时间戳（`Date`）、服务端生成的当前日志追踪编号（`Trace-ID`）完全可以不同，只要业务状态不发生多余变更即可。
- **谬误 2：“只要加了幂等键，系统就彻底实现了‘精确一次（Exactly-Once）’，永远不会出错了。”**
  - **纠正**：在分布式系统中，没有跨越任意物理异构系统的无条件 Exactly-Once。幂等性要求底层存储能够原子地协同记录键与业务状态；如果业务还涉及到发送物理信件、调用没有幂等键的第三方银行接口，网络重试依然会产生外部不可逆副作用。
- **谬误 3：“重试次数越多越好，反正有幂等保护，超时了就一直重试。”**
  - **纠正**：哪怕有幂等保护，每一次重试也会消耗网络带宽、建立 TCP 连接、占用服务端的连接池与线程资源。不设上限的重试依然会引发重试风暴和拒绝服务（DoS）。

---

## 10. 现阶段可暂缓深入的内容 (What You Can Ignore — For Now)

- 分布式多数据库环境下的两阶段提交（2PC）协议（将在 `M18` 深入）；
- 基于分布式日志（如 Kafka）的消费者偏移量与 Exactly-Once Stream Processing 语义（`M18` 主题）；
- gRPC 核心代码中透明重试与 Hedging 请求的高级源码实现。

---

## 11. 渐进式支持阶梯 (Progressive Support Ladder)

### Checkpoint Investigation

#### Question
在 `activity_l16_02.py` 的 Phase 2 中，为什么在服务端模拟丢弃了第 1 次调用的响应后，客户端发起的后续重试没有导致计数器变成 2 或 3，而是恒定保持在 1？

<details>
<summary>Hint 1</summary>
审视 `rpc_fixture.py` 中 `IdempotencyStore.execute_with_idempotency` 的代码。查看其 SQL 事务是如何开启的？使用了什么级别的锁？
</details>

<details>
<summary>Hint 2</summary>
代码使用了 `BEGIN IMMEDIATE`，并首先尝试在同一个事务中查询 `idempotency_keys` 表。如果发现该键已处于 `COMPLETED` 状态，它会做什么？是否还会调用业务变更函数？
</details>

<details>
<summary>Expected Observation</summary>
当第 2 次和第 3 次重试携带相同的键到达时，服务端在事务内命中了已存在的 `COMPLETED` 记录，直接提取并返回了之前缓存的响应结果，完全跳过了业务计数器的递增操作。
</details>

<details>
<summary>Full Explanation</summary>
这正是合成幂等性的基石。客户端在每次重试中都显式携带了相同的意图指纹（Idempotency Key）。服务端将“键的登记”与“计数器的变更”置于同一数据库事务中。第一次请求成功提交后，数据库中已永久固化了该键与该操作的执行结果。后续的重试请求在服务端被识别为“已履约历史的重放”，服务端安全地短路业务计算，直接向客户端投递已完成的事实。这使不确定的网络重试在状态层面上被降维成了良性的幂等读取。
</details>

---

## 12. 架构图解与可视化 (Visual Specification)

### FIG-M16-02 — Retry Amplification vs. Jittered Backoff & Idempotency

```
====================================================================================================
           FIG-M16-02: RETRY AMPLIFICATION vs. JITTERED BACKOFF & IDEMPOTENCY
====================================================================================================

[ 1. NAIVE RETRY WITHOUT BACKOFF / IDEMPOTENCY ]
Client:   ───(Req 1)───> [ Server Overloaded ] ──> Executes Side Effect 1 (Counter=1)
Client:   ───(Req 2)───> [ Server Overloaded ] ──> Executes Side Effect 2 (Counter=2) [CORRUPTION!]
Client:   ───(Req 3)───> [ Server Crashes ]    ──> Cascading failure!

----------------------------------------------------------------------------------------------------
[ 2. EXPONENTIAL BACKOFF WITH FULL JITTER ]
Attempt 1: Fail ──> Sleep Uniform(0, min(Cap, Base * 2^0))
Attempt 2: Fail ──> Sleep Uniform(0, min(Cap, Base * 2^1))  (Traffic smoothed over time)
Attempt 3: Fail ──> Exhaust Budget -> Fast Failure (Protects downstream system)

----------------------------------------------------------------------------------------------------
[ 3. ATOMIC IDEMPOTENT TRANSACTION (ONE SQLITE CONNECTION PER ATTEMPT) ]
Client (Req, Key=K)               SQLite DB (BEGIN IMMEDIATE)                  State Counter
       │                                     │                                       │
  [Attempt 1] ──────────────────────────────>│ INSERT INTO keys (K, 'IN_PROGRESS')   │
       │                                     │ UPDATE counter = counter + 1 ────────>│ [Counter = 1]
       │                                     │ UPDATE keys SET status='COMPLETED'    │
       │                                     │ COMMIT                                │
       ✘ (Response dropped)                  │                                       │
       │                                     │                                       │
  [Attempt 2] ──────────────────────────────>│ SELECT status FROM keys WHERE key=K   │
  (Same Key K)                               │ -> Found COMPLETED!                   │
       │                                     │ -> SKIP Counter Mutation! ─────────── ✘ (No change!)
       │<── Return Cached Result (OK) ───────│                                       │
════════════════════════════════════════════════════════════════════════════════════════════════════
* CORE LESSON: A retry without idempotency converts network ambiguity into permanent data corruption!
====================================================================================================
```

---

## 13. 达标退出标准 (Exit Criteria)

- 能准确推导并阐述微服务调用链中的重试放大效应及防范准则；
- 能给出全抖动指数退避算法的数学公式并解释为什么全抖动能消除雷鸣冲击；
- 能阐述自然幂等与人工合成幂等的本质区别，并指出为什么幂等键必须与业务状态绑定在单次原子事务边界中；
- 实测完成 `activity_l16_02.py`，证明不安全路径出现重复执行，而受保护路径在重试与并发下严格保持计数器恒为 1。

---

## 14. 规范能力映射 (Competency Mapping)

- **Primary**:
  - `Trace`: 准确跟踪带有幂等键的重试请求在服务端事务、唯一键锁表与缓存命中分支中的状态流转；
  - `Judge`: 在面对具体的业务场景（如扣款、发货、改名、查询）时，准确判断其天然幂等性还是需要合成幂等屏障；
  - `Explain`: 清晰向同行或架构师解释为什么缺乏幂等保护的盲目重试会导致生产级数据腐化。
- **Secondary**:
  - `Correctness`: 证明幂等状态迁移在有故障注入的真实代码中保持系统不变式成立。

---

## 15. 核心概念引用与重温 (Concepts & Revisits)

- **`EC-CON-005 Interface (接口)`**: 强化接口契约（Interface Contract）——一个好的远程接口设计必须在其方法签名中显式纳入超时、幂等键与错误模型；
- **`EC-CON-008 Invariant (不变量)`**: 幂等性的本质是系统状态机的状态不变式（$f(f(x)) = f(x)$）；
- **`EC-CON-003 Representation (表示)`**: 幂等键、参数序列化与错误码在网络线缆上的二进制/JSON 表示；
- **`EC-CON-007 Specification (规格)`**: 严格定义调用规范（超时预算、最大尝试次数与退避策略）。

---

## 16. 出处与现时性 (Provenance & Currentness)

1. **Marc Brooker (2015)**: *Exponential Backoff And Jitter*, AWS Architecture Blog. 现代云计算与分布式网络中退避算法的工业标准实证。
2. **Saltzer, Reed, & Clark (1984)**: *End-to-End Arguments in System Design*, ACM TOCS. 深刻指出底层协议无法完全代替端到端应用层可靠性。
3. **RFC 9110 (HTTP Semantics, June 2022)**: Section 9.2 (Idempotent Methods).
4. **IETF Internet-Draft**: `draft-ietf-httpapi-idempotency-key-header-07` (*The Idempotency-Key HTTP Header Field*, 2024). 现行微服务与 API 领域广泛参考的工业模式草案（注：非正式 RFC）。

---

## 17. 严格推论边界 (Exact Inference Limits)

- 实验中所证明的幂等性保证，**仅在当前单机 SQLite 数据库排他锁与本地事务边界内绝对成立**，**绝不能外推宣称能够保证任意第三方分布式微服务或跨云跨机房系统的无限 Exactly-Once**；
- 幂等键表的保留期（TTL）在物理上是有限的，**一旦超过保留期被后台清理，延期到达的重试请求仍会作为新请求重新执行**；
- 指数退避与全抖动仅能减轻瞬时拥塞，**若服务端容量出现根本性的资源耗尽（如磁盘满或数据库死锁），任何重试算法最终都会耗尽预算并失败**。
