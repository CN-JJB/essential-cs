# L11-01｜TLS 1.3 握手、服务身份认证与信任边界

## 学习者问题

当你在浏览器中访问 `https://bank.example.com` 时，地址栏显示了一把“安全锁”。这把锁究竟保证了什么？

你可能听过“HTTPS 能够加密通信并防止窃听”。但如果网络中间有人搭建了一个代理服务器，或者伪造了 DNS 解析将你的流量引向恶意服务器，TLS 是如何发现并阻止假冒者的？为什么仅仅拥有一个受信任机构签发的有效证书，不足以证明一台服务器就是 `bank.example.com`？如果在开发测试时随手加上 `verify=False` 或 `curl -k`，你实际上撕开了一个多大的安全漏洞？

## 为什么重要

在现代系统设计与网络安全实践中，加密通信已经从“可选的高级特性”变成了基础设施的绝对基石。然而，许多工程师对 TLS 的理解仍然停留在“明文变乱码”的粗浅认知上，导致系统暴露在严重的安全威胁之下：
- **混淆传输加密与身份认证**：误以为“只要启用了加密通道就安全”，不知道若没有对服务端身份（Subject Alternative Name, SAN）进行严格校验，中间人攻击者（Man-in-the-Middle, MitM）完全可以与客户端建立合法的加密连接，在明文状态下捕获所有流量；
- **混淆信任链与身份匹配**：误以为“只要证书是由正规 CA 签发的就一定可信”，忽视了 CA 签发的仅仅是“公钥与特定域名的绑定声明”；若拿着给 `attacker.com` 签发的正规证书去冒充 `bank.example.com`，如果不比对域名就会全盘失守；
- **滥用跳过验证开关**：在遇到自签名证书、内网域名解析错误或过期报警时，直接在代码中设置 `verify=False`、`check_hostname=False` 或在命令行加上 `-k`，使整个系统的信任机制形同虚设；
- **泛化安全保障范围**：误以为 TLS 能保护数据库防注入、服务器防勒索或代码免受越权漏洞攻击，忽视了安全协议具有严格的物理与逻辑边界。

要建立现代网络安全体系的精确世界模型，必须深入理解 TLS 1.3 的握手设计、X.509 证书的验证状态机以及信任锚点的工程约束。本课正式复访核心系统概念：**信任边界（Trust Boundary，EC-CON-017）**、**接口（Interface，EC-CON-005）** 与 **故障（Failure，EC-CON-010）**。

## 学习目标

完成本课后，你应能：
- **Explain**：准确阐述课程所采用的**新鲜、证书认证、(EC)DHE 型 TLS 1.3 握手**如何建立流量密钥并提供前向保密；同时说明 TLS 1.3 已移除静态 RSA key exchange，但 PSK-only `psk_ke` 路径不能被误写成同样的 DHE 前向保密；
- **Trace**：清晰追踪 X.509 证书链验证的两大独立判定支柱：从叶子证书向上回溯至本地受信任根证书（Root CA Trust Anchor）的**路径校验（Path Validation）**，以及比对请求域名与证书扩展项的**主体备用名称（SAN, Subject Alternative Name）身份比对**；
- **Observe**：使用 `labs/foundations/m11/tls_fixture.py` 运行由测试根 CA 签发的受控 TLS 1.3 服务，在零跳过标志下分别触发并观测成功握手、服务身份不匹配拒绝和未受信任根证书拒绝三种受控状态，并记录实际运行时 disposition；
- **Judge**：批判性评估“为什么任何生产代码严禁使用 `verify=False` / `-k`”，论证跳过验证不仅是关闭告警，而是彻底废除了系统的**信任边界（EC-CON-017）**；
- **Judge**：清晰划定 TLS 的安全边界：TLS 能提供受协议与验证策略约束的机密性、完整性与端点认证能力，但不自动保证端点内存安全、应用业务正确性、授权策略或本地信任锚本身未被错误配置/攻破。

## 前置知识

- **M10（硬前置）**：TCP 连接、端点抽象与三次握手字节流；
- **M06（软上下文）**：操作系统进程上下文与权限模型；
- 核心概念复访：**信任边界（Trust Boundary，EC-CON-017）**、**接口（Interface，EC-CON-005）** 与 **故障（Failure，EC-CON-010）**。

---

## Mental Model｜TLS 1.3 握手与 X.509 信任链

### 1. TLS 1.3 的握手交互与前向保密

在本课的课程 fixture 中，TLS 1.3 建立在 TCP 连接之上，并使用**新鲜证书认证 + (EC)DHE key share** 这条常见路径来演示流量密钥与前向保密。TLS 1.3 已移除旧式静态 RSA key exchange，但这不等于“TLS 1.3 的每一种 PSK 模式都强制 DHE”：RFC 9846 仍区分 DHE-based 与 PSK-only key establishment。RSA 也仍可作为证书签名算法的一部分，不能把“移除 RSA key exchange”误写成“TLS 1.3 禁止 RSA 证书/签名”。

```
Client                                                              Server
  |                                                                   |
  |  1. ClientHello                                                   |
  |     + Supported Cipher Suites (AEAD only)                         |
  |     + Key Share (Client ephemeral ECDHE public key)               |
  |     + Server Name Indication (SNI)                                |
  | ----------------------------------------------------------------> |
  |                                                                   |
  |  2. ServerHello                                                   |
  |     + Selected Cipher Suite (actual negotiated AEAD suite)       |
  |     + Key Share (Server ephemeral ECDHE public key)               |
  |     {EncryptedExtensions}                                         |
  |     {Certificate} (Server X.509 Certificate Chain)                |
  |     {CertificateVerify} (Signature over handshake transcript)     |
  |     {Finished} (HMAC over entire handshake)                       |
  | <---------------------------------------------------------------- |
  |                                                                   |
  |  [Client verifies Server Certificate Chain & SAN identity]        |
  |  [Client verifies CertificateVerify signature]                    |
  |  [Client derives Symmetric Keys: Application Traffic Keys]        |
  |                                                                   |
  |  3. {Finished}                                                    |
  | ----------------------------------------------------------------> |
  |                                                                   |
  | <========== 1-RTT Handshake Complete: Secure Channel ===========> |
  |                                                                   |
  |  4. {Application Data: e.g. HTTP/1.1 or HTTP/2 over this TLS/TCP} |
  | <===============================================================> |
```

> **层次边界**：上图只描述 TLS-over-TCP 的课程路径。HTTP/3 不运行在这条 TCP 连接上；它使用 QUIC，并把 TLS 1.3 key establishment 集成进 QUIC。HTTP/3/QUIC 的机制比较留到 L11-03。
> **什么是前向保密（Forward Secrecy）？**
> 旧式静态 RSA key exchange 会让“未来拿到长期 RSA 私钥”具有解密历史录制会话的风险。课程采用的 TLS 1.3 (EC)DHE 路径把会话秘密建立在临时 key share 上，因此长期认证私钥的未来泄露**本身**不足以恢复过去会话的流量密钥。
>
> 这个结论有边界：TLS 1.3 还定义 PSK/resumption 路径，其中 `psk_dhe_ke` 使用 DHE，而 PSK-only `psk_ke` 不提供同样的 DHE 前向保密。课程也不声称实现一定在某个时刻“立即擦除全部临时私钥内存”；那是实现/运行时证据，而不是协议接口保证。

### 2. X.509 证书的双支柱验证模型

本课把证书型 TLS 服务认证拆成两层核心检查：PKIX 路径/信任策略，以及应用 service identity 匹配。完整客户端还可能叠加其他证书、算法或本地策略检查，因此这是一套教学分层模型，而不是穷尽所有验证步骤：

```
                              X.509 证书校验状态机
                                       |
                   +-------------------+-------------------+
                   |                                       |
                   v                                       v
         [支柱 1: 路径有效性校验]                [支柱 2: 服务主体身份比对]
         Path / Chain Validation                 Identity / SAN Validation
                   |                                       |
        - 本地/专用 Trust Anchor Policy         - 应用给出的 reference service identity
        - 上级证书对下级证书签名的合法性        - 提取叶子证书的 Subject Alternative Name
        - 证书链中各项有效期（Not Before / Not After） - 检查期望主机名是否在 SAN 列表内
        - X.509 扩展/策略约束（BasicConstraints, - RFC 9525: service identity 只用 SAN-ID
          KeyUsage, ExtendedKeyUsage）                     |
                   |                                       |
                   +-------------------+-------------------+
                                       |
                        两个支柱必须同时满足 (AND)
                                       |
                         +-------------+-------------+
                         |                           |
                     全部通过                     任一失败
                         |                           |
                         v                           v
                  [身份验证成立: 继续握手]   [验证失败: TLS 客户端拒绝]
                                             (记录实际 ssl/TLS disposition)
                                             不自动回退到跳过验证
```

这两大支柱的职责绝对不能相互替代：
1. **支柱 1（路径与信任锚）**：判断叶子证书能否在客户端指定的 trust-anchor/policy 下建立一条可接受的 PKIX 路径，并检查适用的签名、有效期与扩展约束。它并不单独证明“这就是应用想连接的那个服务”；
2. **支柱 2（服务身份比对）**：RFC 9525 定义 application service identity 的表示与验证规则。客户端把应用上下文提供的 reference identity 与证书 SAN 中适用的 presented identity 比较；不能把 SNI、URL 字符串和证书路径验证混成同一件事。只做路径验证、不做服务身份比对，会接受“在信任链上有效但属于别的服务”的证书。

---

## Mechanism｜从测试 PKI 到零跳过实验验证

### 1. X.509 v3 扩展与 RFC 5280 规范约束

在现代操作系统和 OpenSSL 3.x 协议栈中，X.509 证书不仅包含公钥与主体名称，更被严格的扩展字段所约束：

| 扩展项名称 (Extension) | 所在证书 | 核心作用与规范规则 (RFC 5280) | 违背后果 |
|---|---|---|---|
| **Basic Constraints** (`2.5.29.19`) | CA / 叶子证书 | CA 证书需要用约束表达其签发角色；叶子证书不能被误标成可签发下级证书。 | 违反路径/扩展策略会导致验证失败；实际错误码/文本属于具体 TLS/PKIX 实现证据。 |
| **Key Usage** (`2.5.29.15`) | 证书 | 当该扩展存在时，它限制证书公钥可执行的操作。课程 TLS 1.3 RSA 叶子用于握手签名，fixture 允许 `digitalSignature`；不能把 TLS 1.2 RSA key-encipherment 用途写成 TLS 1.3 的普遍要求。 | 具体验证结果取决于证书用途、应用 profile 与实现策略。 |
| **Extended Key Usage (EKU)** (`2.5.29.37`) | 叶子证书 | 若 EKU 存在，它进一步限制用途；课程 fixture 显式包含 `serverAuth`。不同应用/profile 可以对 EKU 是否必须存在施加额外规则。 | 不能把本课程 fixture 的 profile 直接泛化为所有 X.509/TLS 客户端的统一要求。 |
| **Subject Alternative Name (SAN)** (`2.5.29.17`) | 叶子证书 | 承载 RFC 9525 service-identity verification 所使用的 presented identifiers。课程 fixture 使用 `DNS:localhost` 与 `IP:127.0.0.1`。 | RFC 9525 不再把 Common Name 当作服务身份来源；reference identity 不匹配时应拒绝该服务身份。 |

### 2. 观察受控测试端点的三条路径

在 `labs/foundations/m11/tls_fixture.py` 中，我们构建了严谨的受控测试套件：
- 服务端加载由课程根 CA 签发的证书（SAN 包含 `localhost` 与 `127.0.0.1`），并在本地回环动态端口（`127.0.0.1:0`）提供 TLS 1.3 监听；
- 客户端严格通过 `ssl.SSLContext(ssl.PROTOCOL_TLS_CLIENT)` 配置，显式加载指定的 CA 信任文件，**严格开启 `check_hostname=True` 与 `verify_mode=CERT_REQUIRED`**；
- 运行三组实验：

```python
# 路径 1: 信任根 CA + 正确的主机名 -> 成功握手
ctx = ssl.create_default_context(cafile=CA_CERT_PATH)
with socket.create_connection(('127.0.0.1', port)) as sock:
    with ctx.wrap_socket(sock, server_hostname='localhost') as ssock:
        assert ssock.version() == 'TLSv1.3'

# 路径 2: 信任根 CA + 错误 reference identity -> TLS 客户端拒绝
ctx = ssl.create_default_context(cafile=CA_CERT_PATH)
with socket.create_connection(('127.0.0.1', port)) as sock:
    try:
        with ctx.wrap_socket(sock, server_hostname='wrong.example.internal') as ssock:
            pass
    except ssl.SSLError as err:
        # 记录当前 Python/TLS backend 给出的验证失败 disposition；不固定错误文本
        pass

# 路径 3: dedicated trust context 不信任课程 CA -> TLS 客户端拒绝
ctx_untrusted = ssl.create_default_context(cafile=UNTRUSTED_CA_PATH)
with socket.create_connection(('127.0.0.1', port)) as sock:
    try:
        with ctx_untrusted.wrap_socket(sock, server_hostname='localhost') as ssock:
            pass
    except ssl.SSLError as err:
        # 记录当前 Python/TLS backend 给出的 path/trust failure disposition
        pass
```

### 3. 为什么严禁使用跳过验证开关？

在开发或排障时，代码审查中如果看到 `verify=False`、unverified TLS context 或 `curl -k` 这类**验证绕过信号**，应立即追问它关闭了哪些身份检查。课程只要求识别这些危险模式，**不提供可复制执行的绕过命令/代码**。

**工程后果分析**：
1. 对常见高层客户端而言，关闭证书/主机名验证会移除原本用于认证服务端身份的关键检查；具体 flag 的语义属于该客户端 API，不能把所有库都概括成同一个内部实现。
2. 一旦客户端接受不受信任/不匹配的服务身份，具有流量劫持能力的攻击者就可能建立一个“加密但认证给错对象”的连接，并读取该连接中客户端发送的敏感应用数据；
3. **“加密”而不“认证”是彻底的假安全**：攻击者用自己的密钥解密客户端发来的数据，记录明文后，再与真正的后端建立 TLS 连接转交请求。在整个过程中，客户端毫无察觉，所谓的安全保护彻底化为乌有。

### 4. 元数据保护与 ECH (Encrypted Client Hello)

在没有成功使用 ECH 的普通 TLS 1.3 路径中，初始 `ClientHello` 的一些字段仍可被链路观察者看到；不要把它描述成“第一包一定等于完整 ClientHello”，因为握手消息与网络分组边界不是同一接口。
这导致了一个关键隐私泄露：`ClientHello` 中的 **SNI（Server Name Indication，服务器名称指示）** 扩展包含明文的主机域名（如 `visited-site.example.com`）。网络沿途的路由器、ISP 或监听者即使无法解密通信内容，也能清晰记录下用户正在访问哪一个网站。

为了解决这个最后的明文元数据泄露问题，现代 IETF 推出了 **ECH（Encrypted Client Hello）** 技术：
- ECH 客户端需要获得 ECHConfig。当前标准定义了通过 SVCB/HTTPS 记录广告配置的路径，也允许其他预配置/分发方式；
- DNS 传输是否额外受保护是部署/隐私体系的一部分，不是“使用 ECH 必须先用 DoH/DoT”的协议前置条件；
- 客户端构建外层公开的 ClientHelloOuter（仅指向中立的前置面向服务商），并将包含真实敏感 SNI 的 ClientHelloInner 加密封装在扩展字段中；
- 成功部署 ECH 时，真实 inner ClientHello/SNI 可以获得额外保护；观察者仍能看到网络层端点、包长、时序、ECH 使用本身以及服务提供商/匿名集相关信息，因此 ECH 不是“隐藏所有元数据”或“彻底消除网络审查”的保证。

---

## Checkpoint Investigation

### Question

某大型金融科技团队在排查微服务内部调用时，发现某台测试环境客户端容器频频抛出如下错误：
`ssl.SSLCertVerificationError: [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: IP address mismatch, certificate is not valid for '192.168.1.100' (_ssl.c:1000)`

一名初级运维工程师提议：“既然是在内网私有集群里跑，直接把证书路径验证和服务身份校验都关掉就行了，内网绝对安全。”

请结合本课所学的 X.509 信任支柱、SAN 扩展与信任边界（EC-CON-017），回答：
1. 为什么该证书会被底层 SSL 引擎拒绝？
2. 为什么运维工程师“内网绝对安全、直接关闭验证”的提议存在致命架构缺陷？
3. 正确且符合规范的修复方案应该是什么？

<details>
<summary>Hint 1</summary>
仔细阅读报错文本中的关键短语：<code>IP address mismatch, certificate is not valid for '192.168.1.100'</code>。这属于 X.509 验证状态机中的哪一个支柱？
</details>

<details>
<summary>Hint 2</summary>
微服务容器如果在签发证书时只在 Common Name (CN) 中填写了域名或 IP，而没有在 X.509 v3 的 SAN 扩展中添加 <code>IP:192.168.1.100</code>，现代 SSL 引擎会如何处理？零信任架构（Zero Trust）如何看待“内网即安全”的假设？
</details>

<details>
<summary>Expected Observation</summary>
该错误发生在支柱 2（主体身份比对）。尽管证书可能由合法的私有 CA 签发，但该证书的 Subject Alternative Name (SAN) 扩展项中未包含 <code>IP:192.168.1.100</code>（或只配置了 DNS 域名而未配置 IP SAN），因此客户端判定对端身份与证书声明不符。关闭验证会导致内网容器失去互信防线，一旦局域网内任意容器被攻破或发生 DNS/ARP 欺骗，整网流量将完全裸奔。正确的解法是：重新生成符合 RFC 5280 规范的证书，将该内网 IP 或对应的内网服务域名显式加入到 SAN 列表中，并在客户端仅配置私有 CA 的受信任根证书文件。
</details>

<details>
<summary>Full Explanation</summary>
这是一个在分布式微服务架构中极为常见的身份认证与安全边界误区：

1. **底层失败机制分析**：
   根据 RFC 9525 的 service-identity 模型，应用给出的 reference identity 需要与证书 SAN 中适用的 presented identity 按该应用协议的规则匹配。具体 Python/OpenSSL 错误类与文本属于运行时证据，不是 RFC 固定输出。
   如果客户端通过 IP 地址（<code>https://192.168.1.100</code>）发起连接，证书的 SAN 中必须包含 <code>IP:192.168.1.100</code> 这一项属性（不能仅依赖传统的 <code>DNS:</code> 或已废弃的 <code>Common Name</code>）。当该 IP 被用作 reference identity 而证书没有匹配的 IP SAN 时，服务身份比对失败；当前运行时会给出相应 TLS verification disposition。

2. **关闭验证的系统性危害（违背 EC-CON-017 信任边界）**：
   “内网等于安全”是典型的过时边界安全论。现代计算系统遵循**零信任（Zero Trust）**架构：网络基础设施始终被视为不可信介质。
   一旦客户端关闭了证书路径与服务身份验证，就移除了这条连接原本依赖的服务认证防线。具有流量劫持能力的内网节点或错误中间层就可能让客户端连接到错误身份并暴露应用数据。

3. **标准工程解法**：
   - **修正证书签发脚本**：使用 OpenSSL 或 <code>cryptography</code> 库为该服务端重新签发证书，在扩展配置中显式声明：
     <code>subjectAltName = IP:192.168.1.100, DNS:service.internal.cluster</code>
   - **分发私有 CA 信任锚**：在客户端配置受信任的 CA 文件（如 <code>ssl_context.load_verify_locations('/etc/ssl/certs/internal_ca.pem')</code>），保持 <code>check_hostname = True</code> 与严格验证开启。
</details>

---

## Common Misconceptions

- **“只要启用了 HTTPS，我的系统就绝对安全，不会被攻击了。”** 错。TLS 仅保护通信信道上的机密性、完整性与端点认证（EC-CON-017）。它绝不保护服务器应用程序本身的漏洞（如 SQL 注入、越权访问、未授权 API 调用），也不保护客户端本地被植入木马或内存被读取。
- **“证书验证就是看 CA 签名对不对，只要签名有效就说明身份匹配。”** 错。CA 签名只能证明“公钥与主体声明的关系得到了 CA 背书”。如果攻击者拿着合法 CA 给 `evil.com` 签发的证书来拦截发往 `bank.com` 的流量，CA 签名同样是有效通过的！必须同时严格校验证书中的 SAN 扩展与请求的目标域名完全一致。
- **“自签名证书加 `verify=False` 是测试阶段的标准常规手段，上线前删掉就行。”** 错。这种不严谨的代码经常会被带入生产环境或遗留在脚本中；严谨的测试应当建立受控的测试专用根 CA（Local Test PKI），在客户端加载该测试 CA 的专用信任上下文，以与本课程验证目标一致的严格模式运行测试（正如我们在 `labs/foundations/m11/tls_fixture.py` 中所实现的）。
- **“TLS 1.3 握手全部内容都是加密的，网络监听者什么都看不见。”** 错。未使用 ECH 时，普通 ClientHello 中的 SNI 可以暴露服务名称；成功使用 ECH 时 inner SNI 可以被保护，但 IP、时序、大小等元数据仍可见，且观察到什么取决于部署。

## What You Can Ignore—for Now

- 椭圆曲线密码学中 Curve25519 与 NIST P-256 的具体标量乘法数学点运算实现；
- OCSP Stapling 与 CRL 证书吊销列表的实时分布式树状检索同步；
- ECH 的密钥配置（ECHConfig）在 DNS SVCB/HTTPS 记录中的二进制 RDATA 编码细节。

## Exit Criteria

- 能阐明 TLS 1.3 移除静态 RSA key exchange 的意义，并说明课程所用 DHE-based TLS 1.3 路径为何具有前向保密，同时指出 PSK-only 路径的边界；
- 能准确列出 X.509 验证状态机的两大独立支柱：路径校验（Path Validation）与主体身份比对（SAN Validation）；
- 能独立运行 `labs/foundations/m11/tls_fixture.py` 并说明三种握手分支（成功、主机名不匹配、未受信任 CA）的观察现象与底层原因；
- 能在代码审查中一眼识别出 `verify=False`、`check_hostname=False` 和 `curl -k` 的安全漏洞，并从信任边界角度论述其工程危害。

## Competency Mapping

- **Primary**: **Explain** (解释 TLS 1.3 前向保密机制、X.509 证书双支柱校验与信任边界约束)
- **Growth**: **Trace** (追踪从 TCP 握手到 ClientHello、ServerHello、证书链校验与会话密钥派生的全链路状态)

## Provenance

- **RFC 9846**: *The Transport Layer Security (TLS) Protocol Version 1.3*, July 2026 — current project authority; obsoletes RFC 8446.
- **RFC 5280**: *Internet X.509 Public Key Infrastructure Certificate and Certificate Revocation List (CRL) Profile*, May 2008.
- **RFC 9525**: *Service Identity in TLS*, November 2023 — obsoletes RFC 6125.
- **RFC 9849**: *TLS Encrypted Client Hello*, March 2026.
