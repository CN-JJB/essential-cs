# L17-01 — 数据放在多台机器上就安全了吗？复制拓扑、确认语义与法定人数交集

> **Canonical Module**: M17 — Distributed Systems: Replication, Consistency & Consensus
> **Canonical Title**: L17-01 — How do I keep data safe across machines?
> **Canonical Predecessors**: Hard `M16`, `M14`; Soft `M09` (Lesson refinement: `L16-01`, `L09-01`)
> **Canonical Primary Competencies**: `Judge`, `Explain`
> **Canonical Concept Revisits**: `EC-CON-016 Durability`, `EC-CON-006 Trade-off`, `EC-CON-011 Caching`
> **First-Home Claims**: 零第一原籍授权（Zero First-Home Claims Authorized）

---

## 1. 学习者核心问题 (Learner Question)

> “既然单机磁盘可能会发生物理损坏或断电，我们把同一份数据写入 3 台独立的服务器，是不是就绝对安全、高枕无忧了？为什么客户端收到了成功响应，在主节点崩溃切换后依然可能丢数据？所谓的法定人数交集（Quorum, $W + R > N$），真的能保证我们一定读到最新写入的值吗？”

在 `M09` 中，我们学习了单机存储引擎如何利用预写日志（WAL）与 `fsync` 换取单机崩溃域内的持久性（`EC-CON-016 Durability`）。单机持久性只覆盖它所声明的本机/存储故障边界；若要容忍更大的故障域，系统通常会引入**多副本复制（Replication）**，但复制本身仍必须说明确认点、存储状态与故障转移规则。

但跨越物理网络后，“复制”绝不等于“天然持久”：
- **确认语义决定已确认写的故障暴露面**：客户端何时收到成功？若某个具名策略在只有 Leader 持有该写时就确认，而后续故障转移只选择未持有该写的副本，则这笔已确认写可能在该**声明的故障/恢复模型**中丢失；
- **法定人数仅保证集合相交**：高中鸽巢原理告诉我们，若写集合大小 $W$ 与读集合大小 $R$ 之和大于总节点数 $N$（$W + R > N$），读写集合必有至少一个重叠节点。但这仅仅是一个**集合相交的纯数学事实**；
- **重叠绝不等于线性一致**：集合交集并不会自动规定“哪个值算完成写”“读者如何选值”或“并发/未决写如何处理”；这些都必须由具名的版本、顺序、冲突与读写协议补充。

本课将带领学习者打破“多副本必然安全”的直觉盲区，推导复制确认契约与法定人数交集的严格边界。

---

## 2. 能力跃迁 (Capability Transition)

| 维度 | 学习前状态 | 学习后状态 |
| :--- | :--- | :--- |
| **心智模型** | 以为“只要把数据存了 3 份，机器怎么宕机都不会丢数据”，把多副本与持久性等同起来 | 建立“确认点 + 存储状态 + 故障模型 + failover 规则共同决定承诺边界”的心智模型；同步/异步标签本身不规定一个通用代价幅度（`EC-CON-006 Trade-off`） |
| **数学推导与边界判断** | 机械背诵 $W + R > N$ 公式，误以为满足公式就自然获得了线性一致性（Strong Consistency） | 能运用鸽巢原理严密推导集合重叠数量 $(W+R)-N \ge 1$，并能清晰指出：**集合相交（Overlap）仅仅是充要条件中的一部分，没有版本仲裁与协调，重叠绝不等于最新值（Overlap $\ne$ Linearizability）** |
| **异常场景诊断** | 面对网络超时或主节点崩溃导致的未决写入（Ambiguous Write），不知数据是否存在 | 能够推演主节点复制后崩溃、从节点落后、并发冲突写这三类典型失效模式，准确界定客户端重试与读修复的责任边界 |

---

## 3. 机制模型 (Mechanism Model)

### (1) 复制拓扑与确认契约 (Replication Topologies & Acknowledgment Policies)

复制系统通常由多个逻辑副本（Replicas）组成。最经典的拓扑是**单主多从架构（Single-Leader Replication）**：所有写操作必须发往 Leader，Leader 将状态变更记录到本地日志后，向 Follower 广播传播。

```
====================================================================================================
                        REPLICATION ACKNOWLEDGMENT POLICIES & FAILOVER
====================================================================================================

 1. ASYNC REPLICATION (异步复制):
    Client ──── Write ───> [ Leader ] (fsync & ACK immediately!) ─── Success ───> Client
                              │
                              ├── (In background, network delay...) ──> [ Follower 1 ]
                              └── (In background, network delay...) ──> [ Follower 2 ]
    * COURSE SCENARIO: if failover promotes a replica without this acknowledged write and no other recovery copy is used, the write is lost in this model.

 2. SEMI-SYNC COURSE SCENARIO (半同步示例；threshold = 1 follower):
    Client ──── Write ───> [ Leader ] (durable-in-model)
                              │
                              ├── Wait for configured threshold ──> [ Follower 1 ]
                              │                                     [ Follower 1 ] ── ACK ──> Leader
                              │
                              └── other followers are outside the ack condition
    Leader ──── Success ───> Client
    * This threshold is scenario data, NOT the universal definition of “semi-sync”.

 3. QUORUM REPLICATION (法定人数复制, W + R > N):
    Write Quorum W=2: Leader + 1 Follower must confirm before client receives Success.
    Read Quorum  R=2: Reader queries 2 nodes. Pigeonhole principle guarantees intersection!
====================================================================================================
```

客户端何时收到 `Success`，定义了系统的持久性契约：
1. **异步课程场景（Async）**：本课定义“Leader 的 modeled local write 完成即可向客户端确认”，Follower 不属于该 ack condition。
   - **可能收益**：在这个策略中，Follower 的慢/不可达不直接进入客户端确认条件；实际延迟仍取决于 Leader 自己的存储、排队与实现。
   - **代价**：若确认时只有 Leader 拥有该写，而后续 failover 选择未持有它的副本且没有其他恢复来源，则该已确认写会在这个模型中丢失。
2. **全同步课程场景（Sync All）**：本课定义“所有 modeled replica 满足确认条件后才向客户端成功”。
   - **可能收益**：在所声明的 durable-replica 与故障模型下，确认点包含更多副本；这不是无条件的“零数据丢失”保证。
   - **代价**：只要某个**被该策略要求确认**的副本不可达，该次写就无法满足这个 ack condition；其他产品/策略可以配置不同同步集合。
3. **半同步课程场景（Semi-Sync）**：本课用“配置 follower threshold=1”作为示例。真实系统可以等待不同的副本集合、数量或状态。
   - 因此必须记录：究竟谁在 ack point 持有数据、其存储是否 durable-in-model，以及 failover 候选规则。

### (2) 为什么读副本是“未受控的缓存”？(EC-CON-011 Caching)

当架构师为了分担读压力，引入“主写从读（Read from Followers）”策略时，实际上是在不知不觉中把 Follower 当成了没有失效通知机制的缓存（重温 **`EC-CON-011 Caching`**）。由于网络延迟、复制排队与应用进度差异（Replication Lag），Follower **可能**落后于当前写入进度；也可能已经追平。用户刚刚写入个人资料，刷新页面却读到了旧资料，这正是复制延迟引发的典型“脏读/陈旧读（Stale Read）”。

### (3) 法定人数数学：鸽巢原理与集合相交 (Quorum Arithmetic)

在无主（Leaderless）或多数派系统中（如 Gifford 1979 提出的 Quorum 机制），总副本数为 $N$，每次写入必须成功写入 $W$ 个副本，每次读取必须从 $R$ 个副本中读取。

**鸽巢原理（Pigeonhole Principle）保证**：
若设定：
$$W + R > N \iff (W + R) - N \ge 1$$
则任意一个包含 $W$ 个节点的写集合 $S_W$，与任意一个包含 $R$ 个节点的读集合 $S_R$，其交集必非空：
$$S_W \cap S_R \neq \emptyset, \quad |S_W \cap S_R| \ge 1$$
这意味着：**读集合中至少存在一个节点，曾经见证过该笔写操作**。

### (4) 铁律：重叠不等于线性一致性 (OVERLAP $\ne$ LINEARIZABILITY)

许多初学者会推导出一个危险的错误结论：“只要 $W + R > N$，系统就自动具备了线性一致性，总能读到最新的值”。**这是严重的认知谬误！**

集合相交只是纯几何/集合论的包含关系，它**完全无法独自解决以下三个致命问题**：
1. **盲目读取无版本区分（Unversioned Blind Read）**：
   假设 $N=3, W=2, R=2$。写者在 $\{N_1, N_2\}$ 写入新值 $v_2$；$N_3$ 依然保留旧值 $v_1$。读者读取 $\{N_2, N_3\}$，重叠节点是 $N_2$。但是，如果数据本身没有单调递增的逻辑版本号（Version），或者客户端只取第一个到达的网络应答包，读者完全可能返回 $N_3$ 上的旧值 $v_1$！**没有版本比较规则，交集毫无意义**。
2. **未决/部分写入（Ambiguous Partial Write）**：
   写者尝试写入 $v_2$（要求 $W=2$）。数据写入 $N_1$ 成功，但尚未送达 $N_2$，写者物理崩溃或网络断开，写操作向客户端返回超时失败（未知状态）。此时，读者 A 读取 $\{N_1, N_2\}$，发现了 $v_2$ 并返回；随后读者 B 在真实时间更晚的时刻读取 $\{N_2, N_3\}$，却读到了旧值 $v_1$！在物理时间上更晚发起的读操作，竟然读到了更早的值，**直接击穿了单调读与线性一致性**。
3. **并发冲突写入（Concurrent Conflicting Writes）**：
   两个客户端并发写：客户端 A 写入 $X=A$ 到 $\{N_1, N_2\}$；客户端 B 写入 $X=B$ 到 $\{N_2, N_3\}$。两者的交集节点是 $N_2$。$N_2$ 究竟该听谁的？如果没有**具名的版本/顺序/冲突解决协议**，集合重叠本身无法决定 $A$ 与 $B$ 谁应胜出；不同副本可能保留不一致状态。共识是某些系统采用的排序机制之一，但不是这条集合论结论唯一允许的实现。

---

## 4. 认知声称层 (Claim Layer)

1. **核心系统不变式 (Core Invariants)**：
   - 副本数量并不自动等同于数据持久性；已确认数据的持久性边界由确认策略（Ack Policy）与故障转移候选者集合严格决定；
   - $W + R > N$ 仅仅保证集合交集非空（$|S_W \cap S_R| \ge 1$）；它本身不是读出最新数据的充分条件；
   - 若要从 quorum read 推出“最新值/线性一致”结论，还必须给出**写完成规则、值的版本/顺序规则、读选择规则，以及并发/失败写的处理协议**。版本号、read repair、共识等是可能机制，不是一套通用必选公式。
2. **具名规范与实证 (Specification & Evidence)**：
   - 经典文献：Gifford (1979) *Weighted Voting for Replicated Data*，奠定了基于权重的法定人数数学基石；
   - **PostgreSQL 18 current docs（Lead recheck 2026-09-05）**：`synchronous_commit` 的有效值包括 `remote_apply`, `on`, `remote_write`, `local`, `off`。所有非 `off` 模式都等待本地 WAL flush；`local` 不等待同步复制；`remote_write` 等待同步 standby 写入其文件系统但不保证 standby OS crash 后的 durable storage；`on` 等待同步 standby flush 到 durable storage；`remote_apply` 进一步等待 replay/apply。`off` 只是不在 success 前等待 local WAL flush，不应描述成“完全异步内存”。这正说明确认语义必须按产品/配置具名。
   - 课程实验：通过 `labs/foundations/m17/activity_l17_01.py` 的确定性逻辑状态机，分别验证异步丢失、无版本陈旧读、部分写入导致的时序倒流反例。
3. **推论边界与当前实践 (Current Practice & Inference Limits)**：
   - 本课的数值算例（如 $N=3, W=2, R=2$）是用于揭示原理的**场景算术**，绝不是生产集群的硬性推荐配置；
   - 真实的物理复制延迟（Replication Lag）是动态波动的，严禁在理论推导中假定一个固定的毫秒数常量。

---

## 5. 观察前预测 (Prediction-Before-Observation)

在运行本课实验代码前，请写下你的预测：

1. **预测 A（异步复制容灾）**：若集群配置为异步复制（`ASYNC`），Leader 确认写入后，在向 Follower 发送日志前瞬间断电。此时选出 Follower 作为新 Leader，该笔写入会发生什么？
2. **预测 B（法定人数读取）**：$N=3, W=2, R=2$。节点 $N_1$ 写入版本 2，$N_2$ 写入版本 2，$N_3$ 保持版本 1。如果读者访问 $\{N_2, N_3\}$ 且不进行版本号对比，读者是否可能读出旧值？
3. **预测 C（未决写入的时序）**：如果一笔写操作在写入 1 台节点后超时放弃，两个先后发起的法定人数读操作，是否可能先读到新值、后读到旧值？

---

## 6. 动手实践活动 (Hands-on Activity)

本课使用课程提供的逻辑状态追踪装置（纯本地 Python 标准库，无网络守护进程，无 Docker）：

```bash
# 1. 运行 L17-01 复制确认与法定人数交集活动
python labs/foundations/m17/activity_l17_01.py

# 2. 运行自动化测试套件验证所有不变式与反例
python -m unittest discover -s labs/foundations/m17 -p "test_*.py"

# 3. 清理生成的观察记录（安全幂等）
python labs/foundations/m17/reset.py
```

### 观察要点：
1. **PART 1**：观察本课 `ASYNC` 场景在 failover 到未复制 follower 时的 `DATA_LOST_ON_FAILOVER`；再观察 `SEMI_SYNC` **threshold=1 的课程场景**在一个被选中 follower 确认后的结果；
2. **PART 2**：观察 $N=3, W=2, R=2$ 计算出的重叠节点列表；
3. **PART 3**：逐一审查三个经典反例的机器判定结果：
   - 反例 1：`stale_read_manifested == True`（证明无版本控制时交集无效）；
   - 反例 2：`linearizability_violated == True`（证明部分写入导致时序倒流）；
   - 反例 3：并发冲突写导致的不同读者观测分裂。

---

## 7. 受控破坏与失效模式 (Controlled Break / Failure Mode)

### 失效示范：将“法定人数重叠”盲目当成“强一致缓存”导致的金融倒流事故

审查以下朴素读取伪代码：

```python
# 致命设计：以为满足 W + R > N 就直接取第一个返回的响应
def get_user_balance(user_id):
    # 配置 N=3, R=2
    responses = quorum_client.read_from_nodes(
        nodes=["node_2", "node_3"],
        key=user_id,
        return_fastest=True  # 致命错误：贪图低延迟，直接返回最先到达的包
    )
    return responses[0].value
```

**失效剖析**：
尽管读者向包含法定人数的节点发起了并发请求，但如果只取 `return_fastest=True`（即响应最快的节点），当落后的节点 $N_3$ 网络延迟比 $N_2$ 更低时，客户端就会直接返回陈旧的旧余额！
甚至，同一用户连续两次刷新页面，前一次由 $N_2$ 先应答（显示扣款后余额 50 元），后一次由 $N_3$ 先应答（显示扣款前余额 100 元），用户将亲眼目睹“钱又变多了”的时间倒流灵异现象。

---

## 8. 学习者实证记录 (Learner Evidence)

学习者必须在 `course/evidence/foundations-m17-evidence-template.md` 的 **Section B 与 Section C** 中如实记录：
1. 运行 `activity_l17_01.py` 捕获的实际场景数据；
2. 异步复制下故障转移导致的丢数据判定日志；
3. 准确填写 $W + R > N$ 鸽巢原理数学验算过程与交集节点；
4. 用自己的语言解释：为什么即便读写集合必定相交，缺少版本控制时依然会读出陈旧数据；
5. 记录对部分写入导致时序倒流反例的因果分析。

---

## 9. 常见认知谬误 (Common Misconceptions)

- **谬误 1：“只要数据在 3 台机器上都有副本，单台机器挂掉就绝对不丢数据。”**
  - **纠正**：取决于**确认点、存储状态、故障模型与 failover/recovery 规则**。例如本课 ASYNC 场景在确认时只有 Leader 持有该写；若 Leader 随后失效且只提升未持有该写的 follower、又没有其他恢复副本，则这笔已确认写在该模型中丢失。
- **谬误 2：“$W + R > N$ 能保证读写集合重叠，所以系统天然保证强一致性（线性一致性）。”**
  - **纠正**：错！集合重叠只保证有一个节点参与了写并且参与了读。如果不携带逻辑版本号或时间戳进行比较，读者无法判断哪份是最新值；如果存在写入中断（部分节点写成功后崩溃），后续两次读取甚至会出现后读的值比先读的值更旧的时序倒流异常。
- **谬误 3：“半同步复制既有异步的高性能，又有同步的零数据丢失。”**
  - **纠正**：半同步只是折中。当半同步指定的从节点网络延迟升高时，主节点写性能会急剧下降；若半同步降级为异步后主节点崩溃，依然会发生数据丢失。

---

## 10. 现阶段可暂缓深入的内容 (What You Can Ignore — For Now)

- 多主复制（Multi-Leader）中的复杂 CRDT（无冲突复制数据类型）数学推导；
- Dynamo 风格系统中的 Merkle Tree 反熵同步（Anti-Entropy）哈希树构建；
- 分布式共识状态机（Raft / Paxos）中日志索引的复杂提交切分（这是 `L17-02` 的核心）。

---

## 11. 渐进式支持阶梯 (Progressive Support Ladder)

### Checkpoint Investigation

#### Question
在一个 $N=5$ 的法定人数配置系统中，若设定写入法定人数 $W=3$，为了保证读写法定人数必定相交，读取法定人数 $R$ 最小必须是多少？如果此时集群中有 2 台机器物理断网，整个系统还能正常提供读写服务吗？

<details>
<summary>Hint 1</summary>
运用鸽巢原理不等式 $W + R > N$。代入 $N=5, W=3$，求解满足不等式的最小整数 $R$。
</details>

<details>
<summary>Hint 2</summary>
当 2 台机器故障后，剩余存活机器数为 $5 - 2 = 3$ 台。比对当前存活节点数与 $W$ 和 $R$ 的门槛要求。
</details>

<details>
<summary>Expected Observation</summary>
代入公式：$3 + R > 5 \implies R > 2$，因此 $R_{\min} = 3$。当 2 台机器断网后，存活节点数正好为 3 台，恰好满足 $W=3$ 与 $R=3$ 的门槛，系统仍可继续读写；但若再有第 3 台机器断网（存活数 2 < 3），系统将无法凑齐法定人数，读写必须阻塞或报错。
</details>

<details>
<summary>Full Explanation</summary>
鸽巢原理告诉我们，只有当写集合与读集合的总大小超过全集大小时，它们才不得不产生交叠。在 $N=5, W=3$ 的场景下，若 $R=2$，则 $W+R=5$，存在写者写入 $\{1, 2, 3\}$ 而读者读取 $\{4, 5\}$ 的完全隔离可能，读者将完全感知不到写入。因此 $R$ 必须至少为 3。同时，这也说明了为什么法定人数机制具备容错能力：在 $N=5, W=3, R=3$ 下，系统可以容忍至多 $5 - 3 = 2$ 台机器宕机。但必须牢记：这仅仅是在存活节点彼此全通时的简化可用性算术，并不代表系统自动获得了处理并发冲突与线性一致性的全套能力。
</details>

---

## 12. 架构图解与可视化 (Visual Specification)

### FIG-M17-01 — Quorum Overlap vs. Linearizability Boundary

```
====================================================================================================
                        FIG-M17-01: QUORUM OVERLAP VS. LINEARIZABILITY BOUNDARY
====================================================================================================

      Cluster Size: N = 3 Replicas { Node 1, Node 2, Node 3 }
      Write Quorum: W = 2 Replicas { Node 1, Node 2 }
      Read Quorum:  R = 2 Replicas { Node 2, Node 3 }

                   Write Quorum (W=2)                    Read Quorum (R=2)
                ┌───────────────────────┐            ┌───────────────────────┐
                │        Node 1         │            │        Node 3         │
                │     [Holds v2=100]    │            │     [Holds v1=50]     │
                │                       │            │                       │
                │               ┌───────┴────────────┴───────┐               │
                │               │      OVERLAP NODE 2        │               │
                │               │   ★ Holds BOTH v1 & v2 ★   │               │
                │               └───────┬────────────┬───────┘               │
                └───────────────────────┘            └───────────────────────┘

 ╔═════════════════════════════════════════════════════════════════════════════════════════════════╗
 ║                         MANDATORY CORE AXIOM: OVERLAP ≠ LINEARIZABILITY!                        ║
 ╚═════════════════════════════════════════════════════════════════════════════════════════════════╝

   Why Overlap ALONE Fails to Guarantee Reading the Latest Value:
   ┌───────────────────────────────────┬────────────────────────────────────────────────────────┐
   │ Missing Component                 │ Consequence / Anomaly                                  │
   ├───────────────────────────────────┼────────────────────────────────────────────────────────┤
   │ 1. No Version / Logical Clock     │ Reader reading {Node 2, Node 3} blindly returns v1     │
   │                                   │ from Node 3 if Node 3 responds first (Stale Read!).    │
   │ 2. Uncoordinated Partial Write    │ Writer wrote Node 1, crashed before Node 2. Later read │
   │                                   │ on {Node 2, Node 3} sees older value (Time Travel!).   │
   │ 3. Concurrent Conflicting Writes  │ Writer A on {1,2}, Writer B on {2,3}. Overlap Node 2   │
   │                                   │ overwritten without consensus (Data Split/Divergence). │
   └───────────────────────────────────┴────────────────────────────────────────────────────────┘
====================================================================================================
```

---

## 13. 达标退出标准 (Exit Criteria)

- 能清晰画出异步复制、半同步复制与同步复制的时序差异，并准确标出主节点宕机时的“数据丢失窗口”；
- 能运用鸽巢原理公式 $(W+R)-N \ge 1$ 证明读写集合必有交集；
- 能不看教材准确列举出“重叠绝不等于线性一致性”的 3 个经典反例（无版本盲读、未决部分写、并发冲突写）；
- 能够在不依赖真实网络集群的情况下，通过运行 `labs/foundations/m17/activity_l17_01.py` 验证上述全部失效模式并产出完整实证报告。

---

## 14. 规范能力映射 (Competency Mapping)

- **Primary**:
  - `Judge`: 在给定业务目标与故障模型下，比较不同 ack policy 的客户端延迟、可用性与已确认写 durability 边界；
  - `Explain`: 用数学与反例严密解释为什么 $W + R > N$ 仅保证集合交集，而不自动保证线性一致性。
- **Secondary**:
  - `Trace`: 完整追踪一次写入在不同确认策略下的生命周期与副本状态分布。

---

## 15. 核心概念引用与重温 (Concepts & Revisits)

- **`EC-CON-016 Durability (持久性)`**: 从单机本地磁盘的 `fsync` 扩展为跨物理主机的主从复制持久性边界；
- **`EC-CON-006 Trade-off (权衡)`**: 复制延迟（Latency）、集群可用性（Availability）与已确认数据持久性（Durability）的三角权衡；
- **`EC-CON-011 Caching (缓存)`**: 揭示异步复制从节点本质上是“未受控的读缓存”，由此产生复制延迟与陈旧读。

---

## 16. 出处与现时性 (Provenance & Currentness)

1. **Gifford (1979)**: *Weighted Voting for Replicated Data*, ACM SOSP. 提出了经典的法定人数加权投票模型。
2. **PostgreSQL 18 current official documentation**（rechecked 2026-09-05）：`synchronous_commit` / synchronous replication semantics，用作产品特定 current-practice 例子，不外推为通用复制规范。
3. **AWS Dynamo 经典论文 (DeCandia et al., 2007)**: *Dynamo: Amazon's Highly Available Key-value Store*, SOSP. 阐述了 Leaderless 拓扑下 $N, W, R$ 配置与读修复机制。

---

## 17. 严格推论边界 (Exact Inference Limits)

- 本实验基于 Python 内存数据结构构造的逻辑副本，**仅用于验证集合相交与确认逻辑的纯数学与协议不变式，绝不可外推宣称为真实的物理网络分区或物理硬件故障**；
- 算例中使用的 $N=3, W=2, R=2$ 仅为教学场景数据，真实工业界分布式数据库会根据可用区分布、跨地域延迟与硬件成本进行高度定制化配置，绝不存在普适的固定节点数定律；
- 测得的本地状态机执行微秒数属于单进程调度开销，**绝不可作为评估跨物理网络复制延迟的性能依据**。
