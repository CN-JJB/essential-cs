# L17-02 — 机器之间如何达成一致？共识问题、Raft 状态追踪与 FLP 边界

> **Canonical Module**: M17 — Distributed Systems: Replication, Consistency & Consensus
> **Canonical Title**: L17-02 — How do machines agree?
> **Canonical Predecessors**: `L17-01` (Replication Topologies & Quorum Overlap)
> **Canonical Primary Competencies**: `Explain`, `Trace`, `Judge`
> **Canonical Concept First-Home vs. Revisit**:
>   - **Core Concept**: `Consensus (共识)` — 核心概念成立，按 Blueprint R10 决议**登记号保持递延（Registry ID Deferred）**，严禁发明任何伪造 ID；
>   - **Canonical Revisits**: `EC-CON-008 Invariant`, `EC-CON-009 Correctness`
> **First-Home Claims**: 零第一原籍授权（Zero First-Home Claims Authorized）

---

## 1. 学习者核心问题 (Learner Question)

> “在没有中心权威控制的分布式网络中，多台平等的机器如何就某个状态变更（例如谁是 Leader、哪笔交易先执行）达成不可动摇的一致共识？如果网络发生了分区（脑裂），系统如何防止两个互相看不见的小集群同时选出两个‘皇帝’？既然著名的 FLP 定理证明了异步网络中不可能有绝对可靠的共识，为什么 Raft、Paxos 却能在今天的工业界大规模运行？”

在 `L17-01` 中，我们推导出了一个关键结论：即使满足法定人数交集，若缺乏协调与排序仲裁，并发写入仍会导致状态分裂。为了让多个节点协同工作犹如一台永不宕机的单机状态机，分布式系统必须解决其终极核心难题——**共识问题（The Consensus Problem）**。

然而，达成共识面临着理论与工程的双重绝壁：
- **正确性（`EC-CON-009 Correctness`）的三大支柱**：共识协议必须在节点可能随时崩溃的苛刻条件下，同时满足一致性（Agreement）、合法性（Validity）与终止性（Termination）；
- **FLP 不可能定理的绝对红线**：1985 年，Fischer、Lynch、Paterson 提出了计算机科学史上最震撼的定理之一：在纯粹异步网络模型中，只要有一台节点存在崩溃停止的可能，任何确定性共识算法都无法保证在所有执行路径上必定终止；
- **Raft 的破解之道与安全性不变量（`EC-CON-008 Invariant`）**：Raft 等现代算法绝没有“推翻”FLP 定理。它们的选择是：**在安全性上绝不妥协（宁可停摆，绝不分脑），而在活性（Liveness）上依赖部分同步假设与随机化超时**。

本课将带领学习者深入共识不变量的微观推演，通过受控的 5 节点逻辑状态追踪，彻底搞清 Raft 领导者选举与日志完备性的精妙防线。

---

## 2. 能力跃迁 (Capability Transition)

| 维度 | 学习前状态 | 学习后状态 |
| :--- | :--- | :--- |
| **心智模型** | 以为“投票少数服从多数”就能解决一切分布式冲突，把多数派选举当成共识的全部 | 建立“安全性（Safety）高于活性（Liveness）”的系统世界观，深刻理解**多数派重叠仅仅是基础，必须结合任期与日志完备性限制才能防止提交丢失** |
| **定理边界认知** | 误以为 FLP 定理意味着分布式共识不可行，或者反过来以为 Raft 的随机化超时“破解/打败”了 FLP | 能够严密陈述 FLP 定理的**异步模型假设**，清晰解释现代系统如何在工程上通过假设“网络大部分时间延迟有界”来实现实际可用，而理论上异步对抗依然可能导致活性暂停 |
| **协议追踪能力** | 只能粗浅复述 Raft 选举概念，无法判定复杂候选人与选民在不同任期与日志索引下的投票决策 | 能熟练运用 Raft **日志更新度比较规则（Log Up-To-Date Rule §5.4.1）**，严谨追踪网络分区断开与重连时每个节点的任期演进与投票裁决 |

---

## 3. 机制模型 (Mechanism Model)

### (1) 共识问题的形式化定义 (Formal Consensus Properties)

在一个由 $N$ 个进程组成的分布式系统中，每个进程提出一个提议值（Proposal），系统必须运行一个协议，满足以下三大正确性属性（重温 **`EC-CON-009 Correctness`**）：

1. **一致性 / 协定性 (Agreement)**：所有未发生故障的正确进程，最终必须决定在**同一个值**上。绝不允许两个正确节点分别决定了不同的值（杜绝双主/脑裂）；
2. **合法性 (Validity)**：如果系统决定了某个值 $v$，则 $v$ 必须是由某个进程实际提议的值（不能凭空捏造虚假状态）；
3. **终止性 (Termination)**：所有未发生故障的正确进程，最终必须能够在有限时间内达成决定（系统不能永久卡死）。

在分布式学术语境中：
- **安全性 (Safety)**：对应 Agreement 与 Validity，意为“坏事永远不会发生”（系统绝不产出分裂或篡改的数据）；
- **活性 (Liveness)**：对应 Termination，意为“好事终究会发生”（系统能够在有限时间内做出决策并对外服务）。

### (2) FLP 不可能定理的真实边界 (The FLP Impossibility Boundary)

1985 年，Fischer, Lynch, & Paterson (FLP) 在其里程碑论文中给出了严格数学证明：

$$\text{Asynchronous Network} + \text{Deterministic Algorithm} + 1 \text{ Crash Failure} \implies \text{No Guaranteed Termination}$$

**FLP 的精确模型假设**：
1. **纯异步消息传递网络**：消息延迟没有已知的固定上限，通信通道可靠（不丢包、不乱序、无拜占庭造假），但可以任意延迟；
2. **确定性算法**：节点的转移函数是完全确定的；
3. **仅需一台节点崩溃停止（Fail-Stop）**：系统其余节点完全无法区分该节点是死掉了还是仅仅极度迟缓。

**必须杜绝的认知误区**：
- **误区 A：“FLP 说明分布式系统做不出共识”**——错！真实物理网络绝大多数时候是有延迟边界的（弱同步/部分同步模型）。
- **误区 B：“Raft 的随机化超时打破了 FLP”**——错！在纯异步对抗模型下，如果网络对手故意将每个节点的定时器与消息延迟精巧交错，节点仍然会发生无限轮次的选票瓜分（Split Vote），导致终止性无法达成。Raft 的随机化只是在实际工程中极大概率消除碰撞，绝不能偷换概念宣称“否定了 FLP 定理”。

### (3) Raft 多数派重叠定理 (Majority Quorum Overlap)

在一个包含 $N$ 个节点的集群中，法定多数派（Majority）定义为至少获得：
$$Q = \lfloor N/2 \rfloor + 1$$
张选票。

**核心数学不变量**：
任意两个多数派集合必定至少共享一个节点！
$$Q + Q = 2\lfloor N/2 \rfloor + 2 > N$$
- 在 $N=3$ 的集群中，多数派为 2。任何两个大小为 2 的集合必定至少交叠 1 台节点；
- 在 $N=5$ 的集群中，多数派为 3。任何两个大小为 3 的集合必定至少交叠 1 台节点。

这条不变量直接保证了 **选举安全性（Election Safety, `EC-CON-008 Invariant`）**：在同一个任期（Term）内，不可能选出两个不同的 Leader。因为每个节点在同一任期内至多投出一票，如果候选人 A 拿到了多数派，候选人 B 绝不可能再凑出另一个多数派。

### (4) 多数派绝非全部：日志更新度限制 (The Log Up-To-Date Rule §5.4.1)

许多人以为“多数派相交”就足以保证 Raft 的全部安全性。**这是严重的以偏概全！**

如果只凭多数派投票，考虑以下灾难场景：节点 $A$ 包含已经向多数派提交的最新日志（Term 2, Index 5），而节点 $B$ 因为网络断开缺少这段日志（Term 1, Index 2）。如果仅仅因为 $B$ 动作快向无知节点拉到了多数票而当选 Leader，由于 Raft 规定“Leader 拥有绝对权威，Follower 必须无条件覆盖自身日志以同步 Leader”，$B$ 当选后将直接把 $A$ 上已经提交的数据彻底抹杀！

为了杜绝这种灾难，Raft 提出了著名的 **领导者完备性（Leader Completeness）** 限制，并在投票协议中硬编码了 **日志更新度规则（Log Up-To-Date Rule §5.4.1）**：

> **选民 $V$ 唯有在候选人 $C$ 的日志“至少与自己一样新”时，才允许投票给 $C$**。

其严格判定条件为：
$$\text{lastTerm}_C > \text{lastTerm}_V \quad \lor \quad (\text{lastTerm}_C = \text{lastTerm}_V \land \text{lastIndex}_C \ge \text{lastIndex}_V)$$

1. 如果候选人与选民最后一条日志的任期不同，**最后任期更大者日志更新**；
2. 如果最后一条日志的任期相同，**最后日志索引更大（日志更长）者更新**。

**关键结论**：
**多数派重叠 + 日志更新度限制 + 提交规则 = 完整的 Raft 领导者完备性证明**。单纯把安全性归功于“多数派”是极其片面的。

---

## 4. 认知声称层 (Claim Layer)

1. **核心系统不变式 (Core Invariants)**：
   - 选举安全性（Election Safety）：每个任期至多只能有一个领导者被成功选举；
   - 领导者只追加（Leader Append-Only）：领导者从不覆盖或截断自己的日志，只向后追加；
   - 日志匹配性（Log Matching）：如果两份日志在某个索引处具有相同的任期，则从头到该索引处的所有日志完全相同；
   - 领导者完备性（Leader Completeness）：如果一条日志在某个任期被提交，那么该条日志必然存在于未来所有更高任期的领导者日志中。
2. **具名规范与实证 (Specification & Evidence)**：
   - 经典理论：Fischer, Lynch, & Paterson (1985) *Impossibility of Distributed Consensus with One Faulty Process*, JACM；
   - 协议原著：Ongaro & Ousterhout (2014) *In Search of an Understandable Consensus Algorithm*, USENIX ATC；
   - 课程实验：通过 `labs/foundations/m17/activity_l17_02.py` 的逻辑状态追踪，验证 $2 \mid 3$ 分区下少数派无法提交、多数派选主推进、以及日志落后候选人被精确拒票（`DENIED_VOTER_LOG_MORE_UP_TO_DATE`）的机器判定事实。
3. **推论边界与当前实践 (Current Practice & Inference Limits)**：
   - 本课的 5 节点演练是**逻辑纸面/状态机记录（Worksheet Records）**，**绝非运行中的真实 3 节点网络服务，更不是一套完整的 Raft 实现**；
   - 生产环境中的 etcd 或 Consul 使用的心跳间隔（如 100ms）与选举超时（如 1000ms）是具体的工程调优参数，绝不能当作通用的计算机科学常量。

---

## 5. 观察前预测 (Prediction-Before-Observation)

在运行本课实验代码前，请写下你的预测：

1. **预测 A（网络分区下的双主抑制）**：一个 5 节点集群被物理切分为 $\{A, B\}$（少数派）与 $\{C, D, E\}$（多数派）。旧 Leader $A$ 位于少数派端。当客户端尝试向 $A$ 写入新数据时，该数据能否在少数派端被正式提交（Commit）？
2. **预测 B（跨任期日志比较）**：选民 $Y$ 的最后一条日志为 `(Term 2, Index 2)`。候选人 $X$ 的日志很长，最后一条为 `(Term 1, Index 10)`。按照 Raft §5.4.1 规则，选民 $Y$ 会把票投给 $X$ 吗？
3. **预测 C（FLP 与随机化）**：如果在一个纯异步模拟网络中，消息延迟被恶意构造为恰好让每一次选举都超时分票，系统是否能保证终止？

---

## 6. 动手实践活动 (Hands-on Activity)

本课使用课程提供的逻辑共识状态验证装置：

```bash
# 1. 运行 L17-02 共识与 Raft 选举状态追踪活动
python labs/foundations/m17/activity_l17_02.py

# 2. 运行自动化测试套件验证 Raft 投票规则与分区判定
python -m unittest discover -s labs/foundations/m17 -p "test_*.py"

# 3. 清理生成的观察记录
python labs/foundations/m17/reset.py
```

### 观察要点：
1. **PART 1 (5 节点分区拓扑)**：
   - 少数派 $\{A, B\}$：验证 `minority_can_commit_new_entries == False`，证明无法凑齐 3 票多数派，旧主被物理剥夺提交权；
   - 多数派 $\{C, D, E\}$：观察候选人 $C$ 获得 3 张选票，在 Term 2 顺利当选新 Leader；
2. **PART 2 (日志更新度判定 §5.4.1)**：
   - 观察 Case A（Term 1, Index 5 vs Term 2, Index 2）：尽管候选人索引更大，但因任期更小被拒票；
   - 观察 Case B（Term 2, Index 1 vs Term 2, Index 2）：任期相同但索引更小，被拒票；
   - 观察 Case C（Term 3, Index 1 vs Term 2, Index 2）：任期更大，成功获投。

---

## 7. 受控破坏与失效模式 (Controlled Break / Failure Mode)

### 失效示范：删除“日志更新度限制”导致的已提交数据遭覆盖灾难

审查以下去除了日志比较的“朴素多数派投票”伪代码：

```python
# 致命设计：只要任期大于当前任期，选民就无脑投票
def vote_naive(voter, candidate):
    if candidate.term > voter.term:
        voter.term = candidate.term
        voter.voted_for = candidate.id
        return True, "VOTE_GRANTED"  # 致命漏洞：完全不检查 candidate 日志是否完整！
    return False, "DENIED"
```

**失效剖析**：
在一个包含 $\{A, B, C, D, E\}$ 的集群中，假设 $A$ 是旧 Leader，已成功将 `x=1` 复制到 $\{A, B, C\}$ 并向客户端确认提交。
此时 $D$ 刚刚从长期网络隔离中恢复（其日志空空如也），但其定时器触发，发起了更大任期（Term 3）的拉票。
在上述朴素投票逻辑下，$D$ 分别向 $D, E$ 和没有最新提交的从节点拉票，竟然凑齐了 3 票当选为新 Leader！
当选后，$D$ 开始处理新请求并向全集群同步，原本已经在 $\{A, B, C\}$ 提交并向用户确认的 `x=1`，将被 $D$ 的空日志强行覆盖！用户将震惊地发现：**已经付款成功的订单被系统完全抹去了**。
这再次证明：**多数派重叠绝不是万灵药，Leader Completeness 必须由 Log Up-To-Date 共同支撑**。

---

## 8. 学习者实证记录 (Learner Evidence)

学习者必须在 `course/evidence/foundations-m17-evidence-template.md` 的 **Section E 与 Section F** 中如实记录：
1. 运行 `activity_l17_02.py` 捕获的实际分区状态与多数派选举结果；
2. 逐行写出 Raft §5.4.1 三组候选人与选民对比的判定结果；
3. 用严密语言阐述：为什么单纯的多数派交集无法阻止数据被落后 Leader 覆盖，日志更新度限制在其中扮演了什么角色；
4. 记录关于 FLP 定理与 Raft 随机化超时的理论辨析。

---

## 9. 常见认知谬误 (Common Misconceptions)

- **谬误 1：“只要有了多数派选举，集群就绝对不会出现脑裂（Split-Brain）。”**
  - **纠正**：多数派只能保证同一个任期（Term）内最多产生一个 Leader。在网络分区时，旧 Leader 可能依然以为自己是主并继续接收读请求；必须结合租约（Lease）或向多数派确认心跳（Read Index / Quorum Read），才能防止旧 Leader 返回陈旧数据。
- **谬误 2：“Raft 使用随机化超时，已经彻底解决了分布式共识，避开了 FLP 不可能定理。”**
  - **纠正**：错。FLP 定理建立在纯异步网络与无界延迟的理论对抗模型之上。Raft 之所以能在现实中高可用，是因为现实网络通常处于弱同步状态（消息延迟绝大多数时候在可控范围内）。若在极端恶劣的异步对抗网络中，随机超时仍然可能反复碰撞，导致活性停滞。
- **谬误 3：“集群节点越多越安全，把集群从 3 节点扩充到 4 节点能提高容错能力。”**
  - **纠正**：错！3 节点集群多数派为 2，可容忍 $3 - 2 = 1$ 台宕机；4 节点集群多数派为 $\lfloor 4/2 \rfloor + 1 = 3$，同样只能容忍 $4 - 3 = 1$ 台宕机。扩充到 4 节点不仅没有提高容错上限，反而增加了通信开销与故障面。

---

## 10. 现阶段可暂缓深入的内容 (What You Can Ignore — For Now)

- 拜占庭容错（BFT）算法（如 PBFT、Tendermint）对恶意伪造与篡改节点的博弈证明；
- Raft 成员变更（Joint Consensus）的双多数派阶段转换细节；
- Raft 日志快照与 InstallSnapshot RPC 的内存映射传输机制。

---

## 11. 渐进式支持阶梯 (Progressive Support Ladder)

### Checkpoint Investigation

#### Question
为什么在 Raft 协议中，选民在比对候选人的日志更新度时，首先比较的是**最后一条日志的任期（Last Log Term）**，而不是**日志的总长度（Last Log Index）**？

<details>
<summary>Hint 1</summary>
思考任期（Term）在分布式状态机中充当了什么角色？更高任期里产生的日志，比起更低任期里产生的日志，在因果顺序上有何意义？
</details>

<details>
<summary>Hint 2</summary>
假设一个节点在 Term 1 接收了 100 条日志，但全部没有提交；而另一个节点在 Term 2 接收了 2 条日志，且已经提交给客户端。谁的权威更高？
</details>

<details>
<summary>Expected Observation</summary>
任期（Term）代表了分布式系统中的逻辑时钟与因果代际。只要一条日志来自更高的任期，就意味着它经历过更新一轮的合法领导者裁决。较低任期内的日志即便数量再多，也很可能是旧未决领导者产生的未提交废弃日志；优先比较任期，才能保证因果更新的代际压制代际更旧的数据。
</details>

<details>
<summary>Full Explanation</summary>
在 Raft 中，Term 相当于逻辑时钟（Lamport Logical Clock）。一个高任期的 Leader 必然是在观察到了低任期状态（或超越了低任期）之后才被合法选出的。如果仅仅比较日志长度，一个处于长期网络隔离并在本地不断产生垃圾未提交日志的旧节点（例如 Term 1, Index 100），其长度会远大于在新任期中严谨提交的节点（例如 Term 2, Index 10）。若以日志长度为准，旧孤岛节点将以荒谬的“日志多”强行成为新 Leader 并清空真正合法的业务数据。因此，Raft 严格规定：任期优先，任期相同方可比对索引。
</details>

---

## 12. 架构图解与可视化 (Visual Specification)

### FIG-M17-02 — Consensus Safety vs. Liveness: Majority Overlap

```
====================================================================================================
            FIG-M17-02: CONSENSUS SAFETY VS. LIVENESS (5-NODE PARTITION TRACE)
====================================================================================================

               5-Node Cluster: { A, B, C, D, E }  (Majority Quorum Q = 3)
           ─────────────────────────────────────────────────────────────────
                               NETWORK PARTITION BOUNDARY
                                          ┃
            MINORITY PARTITION (2 Nodes)  ┃  MAJORITY PARTITION (3 Nodes)
                   { Node A, Node B }     ┃       { Node C, Node D, Node E }
                                          ┃
    Term 1 Leader: Node A                 ┃  Node C times out, initiates Term 2 election.
    Attempts write: reaches {A, B}        ┃  Candidate C requests votes from {D, E}.
    Votes obtained: 2 / 5                 ┃  Votes obtained: 3 / 5 (MAJORITY WON!)
                                          ┃
    ┌───────────────────────────────────┐ ┃  ┌───────────────────────────────────┐
    │ ★ SAFETY PRESERVED ★              │ ┃  │ ★ LIVENESS PRESERVED ★            │
    │ 2 < 3 Quorum Threshold            │ ┃  │ 3 >= 3 Quorum Threshold           │
    │ CANNOT COMMIT NEW ENTRIES!        │ ┃  │ CAN ELECT LEADER & COMMIT!        │
    │ (Split-brain successfully denied) │ ┃  │ (Progress continues in majority)  │
    └───────────────────────────────────┘ ┃  └───────────────────────────────────┘
                                          ┃
 ╔═════════════════════════════════════════════════════════════════════════════════════════════════╗
 ║              CRUCIAL BOUNDARY: MAJORITY OVERLAP ALONE IS NOT THE WHOLE PROOF!                   ║
 ╚═════════════════════════════════════════════════════════════════════════════════════════════════╝
   * Majority Overlap proves: At most ONE leader per term (Election Safety).
   * But Leader Completeness (protecting committed history) REQUIRES:
     Majority Overlap + Log Up-To-Date Restriction (§5.4.1) + Leader Commit Rules!
   * FLP Impossibility Boundary:
     Raft preserves Safety under full asynchrony. Liveness requires partial synchrony.
     Randomized election timeouts DO NOT disprove or defeat the FLP theorem!
====================================================================================================
```

---

## 13. 达标退出标准 (Exit Criteria)

- 能准确陈述共识问题的三大形式化属性（Agreement, Validity, Termination）并指出哪些对应 Safety、哪个对应 Liveness；
- 能严密背诵 FLP 不可能定理的 3 个模型假设，并讲清为什么 Raft 的随机化超时没有打破 FLP；
- 能手算任意 $N$ 节点集群的多数派阈值 $\lfloor N/2 \rfloor + 1$ 并证明任意两个多数派必相交；
- 能熟练运用 Raft §5.4.1 规则准确判断任意选民对任意候选人的选票仲裁结果；
- 能够在不启动真实网络集群的前提下，独立运行 `activity_l17_02.py` 并对产出的判定结果做出无误的因果解释。

---

## 14. 规范能力映射 (Competency Mapping)

- **Primary**:
  - `Explain`: 清晰阐述分脑危害以及 Raft 如何结合多数派投票与日志规则保证安全性不变量；
  - `Trace`: 完整追踪一个包含 5 个逻辑节点的集群在网络分区与任期晋升过程中的状态迁移与投票判定；
  - `Judge`: 评估共识协议在面对 FLP 定理时的设计妥协，权衡异步安全性与部分同步活性。
- **Secondary**:
  - `Diagnose`: 诊断由于缺乏日志完备性保护导致的旧 Leader 覆盖已提交数据灾难。

---

## 15. 核心概念引用与重温 (Concepts & Revisits)

- **Core Concept: `Consensus (共识)`**: 多节点在不可靠网络下就单项值达成一致的根本机制（Registry ID Deferred）；
- **`EC-CON-008 Invariant (不变量)`**: Raft 的 Election Safety（单任期至多单 Leader）与 Leader Completeness（已提交日志永不丢失）；
- **`EC-CON-009 Correctness (正确性)`**: Agreement（一致性）、Validity（合法性）与 Termination（终止性）。

---

## 16. 出处与现时性 (Provenance & Currentness)

1. **Fischer, Lynch, & Paterson (1985)**: *Impossibility of Distributed Consensus with One Faulty Process*, JACM 32(2): 374-382. 分布式系统最经典的理论不可能界碑。
2. **Ongaro & Ousterhout (2014)**: *In Search of an Understandable Consensus Algorithm*, USENIX Annual Technical Conference. 奠定现代工程共识基石的 Raft 论文。
3. **etcd 3.5 / 3.6 Raft 官方实现规范**（CoreOS / CNCF）：工业界生产级 Go 语言 Raft 实现对 §5.4.1 日志更新度的严密落地。

---

## 17. 严格推论边界 (Exact Inference Limits)

- 本课演练的 5 节点分区与日志判定，**属于离散逻辑状态推演，绝不是在真实网络接口上注入丢包或模拟物理分脑**；
- 教学模型假定节点处于非拜占庭（Crash-Stop / Fail-Recovery）故障模型，**绝不可外推宣称该模型能防御恶意攻击、伪造签名或内存位翻转等拜占庭故障**；
- 绝不可将 Raft 论文推荐的 150ms–300ms 超时区间作为所有网络环境的放之四海皆准的黄金定律。
